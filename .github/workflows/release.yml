name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build Apple Silicon Mac binary
        run: |
          mkdir -p dist
          CGO_ENABLED=1 go build -o dist/unitevault-binary-arm64 ./cmd/unitevault

      - name: Create macOS .app Bundle
        run: |
          mkdir -p "dist/UniteVault.app/Contents/MacOS"
          mkdir -p "dist/UniteVault.app/Contents/Resources"
          cp dist/unitevault-binary-arm64 "dist/UniteVault.app/Contents/MacOS/UniteVault"
          chmod +x "dist/UniteVault.app/Contents/MacOS/UniteVault"
          cp assets/AppIcon.icns "dist/UniteVault.app/Contents/Resources/AppIcon.icns"
          cat <<EOF > "dist/UniteVault.app/Contents/Info.plist"
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>UniteVault</string>
              <key>CFBundleIconFile</key>
              <string>AppIcon</string>
              <key>CFBundleIdentifier</key>
              <string>com.unitevault.app</string>
              <key>CFBundleName</key>
              <string>UniteVault</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>0.0.37</string>
              <key>LSUIElement</key>
              <true/>
          </dict>
          </plist>
          EOF

      - name: Ad-hoc Sign macOS .app Bundle
        run: |
          codesign --force --deep --sign - "dist/UniteVault.app" || true

      - name: Zip macOS .app Bundle
        run: |
          (cd dist && zip -r UniteVault-mac-arm64.app.zip UniteVault.app)

      # The Settings/tray GUI is built on Fyne, which requires CGO (it binds
      # to native OpenGL/Win32 APIs). Cross-compiling a CGO-enabled Windows
      # binary from this macOS runner needs a mingw-w64 cross C toolchain.
      - name: Install Windows cross-compile toolchain (mingw-w64)
        run: brew install mingw-w64

      - name: Build Windows binary & Create ZIP
        run: |
          CGO_ENABLED=1 GOOS=windows GOARCH=amd64 \
            CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++ \
            go build -ldflags "-H=windowsgui" -o dist/UniteVault.exe ./cmd/unitevault
          (cd dist && zip UniteVault-windows-amd64.zip UniteVault.exe)

      - name: Release artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/UniteVault-mac-arm64.app.zip
            dist/UniteVault-windows-amd64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
