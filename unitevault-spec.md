# UniteVault 仕様書

**アプリケーション名**：UniteVault（表示名） / `unitevault`（CLI・実行ファイル・フォルダ名等、全小文字表記が必要な箇所で使用）

## 1. 概要

### 1.1 目的
Obsidian Vault（Markdownファイル群）を Mac / iPhone 間で安全に利用しつつ、最終的なバックアップ先として Google Drive を用いる。デバイス間の同期は既存の iCloud に任せ、ファイル内容の競合検出・解消は独自ログ機構によって行い、Google Drive へは一方向のミラー転送を行う。

### 1.2 背景・課題
- Obsidian 無料版には複数デバイス間の公式同期機能がないため、Mac/iPhone 間は iCloud フォルダで同期している。
- iCloud 上に置いた Vault の内容を Google Drive にも複製したいが、公式アプリ・API キー発行を使わずに実現したい。
- タイムスタンプのみによる同期判定は、時刻ズレやオフライン編集時の競合に弱い。
- git を iCloud 同期フォルダ内に直接置くと、`.git` 内部のバイナリファイル（index・packfile 等）が iCloud のオンデマンドダウンロード機構と衝突し、リポジトリ破損のリスクがある。
- Google Drive をブラウザ自動操作（Playwright 等）で操作する方式は、UI変更への追従コスト・利用規約上のリスク・iOS上で動作しない、という理由により不採用とする。

### 1.3 採用する基本方針
| 項目 | 方式 |
|---|---|
| Mac ⇔ iPhone ⇔ Windows 間の同期 | iCloud Drive（既存のまま。Windowsには iCloud for Windows を追加導入） |
| 競合検出・履歴管理 | 独自の「デバイス別diffログ」＋ 3-way merge（`git merge-file`を部品として利用） |
| Google Drive への転送 | rclone による一方向ミラー（`rclone sync`） |
| Google Drive 認証 | rclone の OAuth ブラウザ連携（APIキー手動発行不要） |
| 依存ツールの自動セットアップ | Git（brew / xcode-select / winget / インストーラー自動起動）および rclone（ユーザー設定領域 `~/.unitevault/bin/` へ自動取得）の全自動検知・インストール（両者が実際に必要となる条件の違いは3.6.3.1節参照） |
| 処理実装言語 | Go（単一の静的バイナリ / macOS `.app` バンドルにクロスコンパイルし、Mac/Windowsにランタイム不要で配布するため） |
| マージ処理・Drive転送の実行主体 | 「プライマリノード」役を1台に固定する方式を基本とする（1.4節参照） |
| 動作形態・GUI | **高コントラスト常駐アイコン対応 メニューバー/システムトレイGUI**。設定要約確認・リモート検証（`rclone listremotes`）・対話的ウィザード（Vault・Remote・Target Path）対応 |
| macOS配布形式 | **`UniteVault.app` バンドル**（.app.zip）および CLI単一バイナリ。アドホック署名付き |
| iPhone側の追加インストール | なし（iCloud同期のみで完結） |

### 1.4 対応デバイスとエンジン方式
- **Mac**：編集端末 兼 デフォルトの「プライマリノード（エンジン担当）」（マージ処理・Google Drive転送を実行）
- **iPhone**：編集端末のみ（iCloud同期以外の処理は行わない、追加インストール不要）
- **Windows PC（複数台可）**：編集端末。それぞれ固有のデバイスIDを持つ「末端デバイス」として扱う（3.2節）。エンジン機能を持たせる場合は3.6節の方式に従う

デバイスの種類・台数は特に制限しない。プライマリノード以外は全て「編集専用の末端デバイス」として同列に扱われ、それぞれが自分専用のログを持つだけでよい（3.2節・3.3節）。複数デバイスが同時にマージ処理・Google Drive転送を実行すると競合するため、**エンジン役（プライマリノード）は基本的に1台に固定する（固定ハブ方式）**。詳細は3.6節を参照。

### 1.5 ライセンス
[MIT License](LICENSE)でオープンソース公開する。コードのコピー・改変・再配布・フォークは自由とする一方、無保証（NO WARRANTY）・無責任（作者はファイル操作やデータの取り扱いに起因する不具合・損害について一切責任を負わない）を明記する。GPL系（コピーレフト、派生物も同ライセンスでの公開を強制）は、フォークされたコードを非公開の派生物として扱う自由まで確保したいという方針とは合わないため不採用とした。

### 1.6 アーキテクチャ移行：Google Drive中心方式への移行（2026-08-27〜）

**現在この節はターゲット（移行後）アーキテクチャの設計を記す。実装は`unitevault-todo.md`のPhase 14〜19として進行中であり、完了するまでは2章・3.5節・3.6節は引き続き旧方式（iCloud Drive上にVaultを直接置く方式）の記述のままとなる。**各Phaseが実装され次第、該当する節を新方式の内容に更新していく。

#### 1.6.1 移行の背景・経緯

現行方式（Vault本体をiCloud Drive上に直接置き、Obsidianがそこへ直接書き込む）には、構造的なリスクがあることが判明した：ObsidianによるVaultファイルへの直接書き込みと、iCloudの内部デーモンによる同期・独自コンフリクト処理（オフライン分岐時、Apple独自のバージョン管理により、ユーザーに見える形でファイルが「ファイル名 2.md」のような別名の複製として保存される。詳細は3.6.1.6節「iCloudの既知の制約」参照）が、**同じファイルに対して同時に働き得る**。このアプリ自身のスキャン・3-way mergeエンジン（3.3節・3.4節）は今回の実装で正しく機能するよう修正したが、iCloud側の独自処理と競合し、意図せず重複ファイルが残る等の可能性が排除できない。

そこで、以下の方針へ移行する：

- **Vault本体はローカル専用フォルダに置く**（Mac: `~/ObsidianVault`、Windows: `%USERPROFILE%\ObsidianVault` をデフォルトとする）。iCloudの影響を受けない場所とすることで、Obsidian自身の編集とiCloudの内部処理が競合するリスクを排除する。
- **PC間（Mac⇔Windows、Mac⇔Mac等）の同期は、Google Drive（rclone）を主経路とする。** 全PCがGoogle Driveへ接続し、このアプリ自身のスキャン・ログ・3-way mergeエンジンがPC間の内容統合を担う（3.3節・3.4節の仕組みをそのまま活用できる）。
- **iPhoneとの橋渡しだけは、引き続きiCloudに依存する**（iOSのバックグラウンド実行制限により、iPhone上でこのアプリ自体を実行する手段が無いため。1.4節参照）。この依存はクラウドの種類を問わず不可避な、iOSプラットフォームの制約であり、Google Drive中心方式へ移行しても解消されない。ただし後述の「iCloud Bridge」方式により、その影響範囲をiPhoneとの橋渡し専用のステージング領域に限定し、Vault本体には及ばないようにする。

#### 1.6.2 新しい構成図

```
Google Drive
    │
    │ rclone（双方向）
    ├──────────────┬──────────────────┐
    │              │                  │
Primary機        Secondary機       Secondary機
(Mac/Windows)    (Mac/Windows)     (Mac/Windows、複数可)
    │
    │ ローカル⇔ローカルミラー（このアプリが管理）
    ▼
iCloud Bridgeフォルダ（iCloud Drive配下）
    │
    │ iCloud（OS標準機能、Apple独自のバージョン管理に依存）
    ▼
  iPhone / iPad
```

Primary機のみがiCloud Bridgeを持つ（iPhoneとの橋渡しは固定ハブ方式のPrimaryが代表して行う。3.6.1.1節の固定ハブ方式という既存方針との整合を保つ）。iCloud Bridgeフォルダの設定は任意（iPhoneを使わない構成なら空でよく、その場合はGoogle Drive同期のみで完結する）。

#### 1.6.3 iCloud Bridge機構

> **実装状況：** 1.6.7節の「Vault Migration」機能により、iCloud Bridgeフォルダへの**初回シードコピー**（Vault移行時の1回限りのコピー）は実装済み。以下に記す**継続的な双方向同期・マージへの組み込み**は`unitevault-todo.md`のPhase 15として未実装。実装されるまでは、iPhone側でのその後の編集は自動的に本体Vaultへ取り込まれない（初回コピー後、明示的な再同期の手段がまだ無い）。

iCloud Bridgeフォルダを、**通常のデバイスと同列の「仮想デバイス」**として扱う。具体的には：

- Primary機が持つ専用の仮想デバイスID（`icloud_bridge_device_id`）を1つ生成・保持する。
- 毎ティック、Vault本体とは別にiCloud Bridgeフォルダも独立にスキャンし、変更を検出する（3.3.0節のハッシュベース変更検出をそのまま再利用）。
- 検出した変更を、Vault本体の`_sync/log-<bridge-id>.jsonl`へ、通常のデバイスと全く同じスキーマで記録する。
- 3.3節の`mergeAndTrackConflicts`はデバイスIDの種類を区別しないため、追加のマージロジック変更なしに、iCloud Bridge由来の変更もPC由来の変更と同列に3-way mergeへ組み込まれる。
- マージ後のVault本体の内容を、iCloud Bridgeフォルダへローカル⇔ローカルでミラーし直す（iPhone側が次にiCloud経由で受け取れるようにする）。

この設計により、iPhoneとの橋渡しで新規に書く必要があるコードは「ブリッジフォルダの独立スキャン」と「ミラーの書き戻し」の2点のみで、3-way merge・コンフリクト解決GUI（3.3.2節）はそのまま流用できる。

#### 1.6.4 Google Driveを全PC間の共有ハブとする変更（実装済み）

これまでGoogle Driveへの転送はPrimaryのみが行う一方向ミラー（3.5節）だったが、新方式ではSecondaryもGoogle Driveへ接続する：

- **Secondary → Google Drive（push）**：自分の`_sync/`（ログのみ）を`rclone copy`（`sync`ではない）でアップロードする。Vault現物ファイルはpushしない - ログエントリの`diff`フィールドに全文が保存されている（3.4節）ため、Primary側のマージにはログだけで十分であり、余計な現物転送を避けられる。`copy`を使うことで、他デバイス分のファイルやPrimaryが公開した最新のマージ結果を誤って削除してしまうことも防ぐ。
- **Primary → Google Drive（pull）**：全デバイスの`_sync/`（ログのみ）を`rclone copy`でローカルへ取得してから、3-way mergeを実行する（3.3節）。`_sync/`のみに限定するのは、Vault現物ファイル全体をpullしてしまうと、Primary自身がこのサイクルで既に編集・確定させたVaultファイルを、Google Drive上の古い内容で誤って上書きしてしまうリスクがあるため。
- **Primary → Google Drive（publish）**：マージ後のVault全体を`rclone sync`でGoogle Driveへミラーする（Primaryのみがこの権限を持つ、という固定ハブ方式の既存方針を維持。変更なし）。
- **Secondary → Google Drive（pull）**：Primaryが公開した最新のマージ結果（Vault全体）を`rclone copy`でローカルへ反映する。

**既知の制約（v1）：** Secondaryのpullは`copy`（追加・更新のみ）であるため、Primaryが削除したファイルは自動的にはローカルから削除されない。また、直近の未確定（デバウンス未安定）なローカル編集が、稀にこのpullによって上書きされる可能性がある。Obsidianが実際に編集中のVaultフォルダに対して破壊的な`rclone sync`は使わない、という安全側の判断による受け入れ済みの制約とする（`unitevault-todo.md`のPhase 16参照）。

#### 1.6.5 同期スケジューリング（交互実行方式）

外部同期（Google Drive同期・iCloud Bridge同期）は、同時に両方実行するのではなく、**60秒間隔の共通ティックで交互に1つずつ実行する**。

- 毎ティック（60秒ごと）：ローカルスキャン・ログ記録・（Primaryのみ）3-way mergeは常に実行する。
- 外部同期タスク（Google Drive同期・iCloud Bridge同期）はリストとして持ち、ティックごとに順番に1つずつ実行するラウンドロビン方式にする。設定されていない同期先（iCloud Bridge未設定等）はリストから除外する。
- 結果として、有効な同期先が2つ（Google Drive・iCloud Bridge）ある場合、**それぞれの実効同期間隔は約120秒**になる。Vault内は基本的にMarkdownファイルでデータ量が小さいため、60秒以内に各同期処理が完了する前提は現実的と判断する。
- 同期先が1つのみ（例：iPhoneを使わずGoogle Driveのみ）の場合は、毎ティック（60秒間隔）でその同期が実行される。
- **将来のTodo**：Vaultのデータ量・ファイル数に応じて、この間隔を自動調整、またはユーザーへ変更を提案する機能（`unitevault-todo.md`参照）。

#### 1.6.6 変わらない部分

- 固定ハブ方式（Primary/Secondaryの役割、3.6.1節）自体は変更しない。iCloud Bridgeを持つのもPrimaryのみ。
- 3-way mergeエンジン・base復元機構（3.3節・3.4節）はそのまま流用する。
- コンテンツ競合のGUI解決フロー（3.3.2節）・Standalone/Syncingステータス（3.6.1.5節）もそのまま流用する（「他のデバイス」の判定にiCloud Bridge仮想デバイスを含めるかどうかは実装時に検討する）。
- Gitへの依存は変わらず必要（3.3.1節の通り、3-way mergeの実装に`git merge-file`を利用するため）。

#### 1.6.7 Vault Migration機能（実装済み）

既存のVault（典型的にはiCloud Drive上に置かれているもの）を、1.6.1節の新方式へ移行するための機能。Settings画面の「Obsidian Vault」セクションに「Migrate Vault to Local Folder...」ボタンとして提供する（常に表示、Vault未設定の初回セットアップ時にも使える）。

**手順：**

1. OS標準のフォルダ選択ダイアログで、移行元のVaultフォルダを選択する。
2. 移行先を`~/<選択したフォルダ名>`（Windowsは`%USERPROFILE%\<選択したフォルダ名>`）に決定し、確認ダイアログを表示する。
3. 確認後、フォルダを移行先へ移動する（同一ボリューム内なら`rename`、iCloud Drive等クロスボリュームな場合は再帰コピー後に元フォルダを削除。コピーが失敗した場合は元フォルダを残したまま中断し、データを失わない）。移行先に既にフォルダが存在する場合は、上書き・マージせずエラーで中断する。
4. Obsidian自身のVault一覧（`obsidian.json`）を、移行元パスに一致するエントリを見つけて移行先パスへ書き換える、ベストエフォート処理を行う（書き換え前に`obsidian.json.bak`としてバックアップを作成する）。該当エントリが見つからない・ファイル形式が想定と異なる等の場合はエラーとして扱い、移行処理自体は中断せず、Obsidian側は手動で開き直すよう案内する。
5. iCloud Driveが検出できる場合（`~/Library/Mobile Documents/com~apple~CloudDocs`または`%USERPROFILE%\iCloud Drive`が存在する）、移行先の内容を`<iCloud Drive>/Obsidian/<フォルダ名>`へ再帰コピーし、`config.ICloudBridgePath`に記録する。このコピーは**移行時点の1回限りのシード**であり、1.6.3節の通り継続的な同期はまだ実装されていない。
6. 移行先パスをVaultパスとして`saveSettingsConfirmed`（既存のSave Settingsと全く同じ処理）へ引き継ぎ、Google Driveリモートが未設定なら設定を促し、初期化（Primary/Secondary判定、初回同期）を行い、常駐同期ループを開始する。

**安全性：** フォルダの移動・Obsidian設定の書き換えは、いずれも同期サイクル進行中は実行できない（3.5.3節の排他制御を利用）。移動先が既に存在する場合や、移動中のエラーは、元のVaultフォルダを変更しないまま中断する。

---

## 2. システム構成図

> **注：本章は現行実装（移行前）の構成を記す。** 1.6節の通りGoogle Drive中心アーキテクチャへの移行を計画中で、移行後の構成図は1.6.2節を参照。`unitevault-todo.md`のPhase 14〜19が完了次第、本章を新方式の内容に更新する。

```
[iPhone: Obsidian]   [Windows PC A]   [Windows PC B]  ...(台数は任意)
        │                  │                │
        └──────────┬───────┴────────────────┘
                    │  iCloud同期（既存の仕組み／Windowsは iCloud for Windows）
                    ▼
     [Mac: iCloud上のVaultフォルダ]  ← 人間が直接編集する「現物」
                    │
                    │ (A) 変更検出 タスクスケジューラ/cron
                    ▼
     [プライマリノード: 同期エンジン (Go製バイナリ)]
    1. Vault内ファイルのハッシュ比較 → 変更ファイル抽出（改行コードはLFに正規化してから比較）
    2. 自デバイスのログ (log-<UUID>.jsonl) に diff を追記
    3. 他デバイスのログ群と突き合わせ（存在する台数分すべて）
       → 競合があれば 3-way merge (git merge-file) を必要な組み合わせ分繰り返す
       → 自動解決できない箇所のみユーザーに提示
    4. マージ結果を Vault現物ファイルに反映
                    │
                    │ (B) マージ完了後、Google Driveへミラー
                    ▼
     [rclone sync] ──▶ [Google Drive: バックアップ用フォルダ]
```

- 「プライマリノード」は基本Mac固定（1.4節）。他デバイスにプライマリ機能を持たせる場合はロック機構が必要（3.6節）。

- Google Drive は **人間が直接編集しない、読み取り専用に近いバックアップ先** として扱う。
- 双方向の複雑な状態管理（rclone bisync 等）はベータ品質のため、今回は不採用。片方向ミラーで十分。

---

## 3. コンポーネント別仕様

### 3.1 Vault本体
- 保存場所：iCloud Drive 上の既存フォルダ（変更なし）
- 役割：常に「最新のスナップショット」として扱う。diffログはこの現物に至るまでの変更履歴を記録するものであり、diffだけでは原本を復元できないため、現物ファイル自体が実質的なスナップショットとなる。

### 3.2 デバイス別ログ
デバイスごとに専用のログファイルを持ち、**各デバイスは自分のログにしか書き込まない**（ログファイル自体が競合対象にならないようにするため）。

#### デバイスIDの決定方式（重要）
ログファイル名にホスト名をそのまま使うと、以下の理由で長期的に問題が起きうる。

- ホスト名の重複（クローンしたPCイメージ、Windowsのデフォルト名`DESKTOP-XXXXXXX`の偶然一致など）
- ホスト名の変更（PC名変更、OS再インストール等で過去ログとの継続性が切れる）
- ファイル名に使えない文字を含む可能性（日本語Windows環境での全角文字・スペース等）

そのため、**デバイスIDとホスト名を分離する**方式を採る。

- 各デバイスで初回実行時に、**ランダムなID（UUID）を1つ生成**し、ローカルの設定ファイル（例：`~/.unitevault/device_id`）に保存する。以後はこのIDを使い続ける。
- ログファイル名はこのUUIDベースにする（例：`log-8f3a1c2e.jsonl`）。
- ホスト名は「人間にとって分かりやすい表示名」として、各ログエントリ内に`label`フィールドで別途持たせる（例：`"label": "win-desktop"`）。ホスト名が後で変わっても、同じUUIDを使い続ける限りログの継続性は保たれる。
- 台数・種類は制限しない。Windows PCが複数台あっても、それぞれが固有のUUIDを持つ限り、ログファイル同士は衝突しない。

- 配置例：`Vault/_sync/log-8f3a1c2e.jsonl`（Mac）、`Vault/_sync/log-3b7e9f01.jsonl`（iPhone）、`Vault/_sync/log-c14d2a88.jsonl`（Windows機A）、`Vault/_sync/log-9e0f5c3a.jsonl`（Windows機B）...
- 形式：1行1エントリの JSON Lines（追記のみ）
- 注意：ログ内の`diff`本文についても改行コードをLFに正規化してから記録する（Windows側での自動変換によるノイズ差分を防ぐため）

#### ログエントリのスキーマ

```json
{
  "device": "8f3a1c2e",
  "label": "mac-mini",
  "seq": 42,
  "path": "Notes/foo.md",
  "base_hash": "xyz789",
  "result_hash": "abc123",
  "diff": "--- a/foo.md\n+++ b/foo.md\n@@ ...",
  "action": "modify",
  "ts": "2026-08-22T12:34:56+09:00"
}
```

| フィールド | 説明 |
|---|---|
| `device` | 初回実行時に生成した固有デバイスID（UUID）。ログファイル名にも使用 |
| `label` | 人間向けの表示名（ホスト名等）。一意性の保証には使わない、表示専用の情報 |
| `seq` | デバイス内での連番（単調増加） |
| `path` | Vaultルートからの相対パス |
| `base_hash` | この変更の前提となった内容のハッシュ（分岐点の特定に使用） |
| `result_hash` | 変更適用後の内容のハッシュ |
| `diff` | unified diff形式の差分本体 |
| `action` | `modify` / `create` / `delete` / `rename` |
| `ts` | 記録時刻（参考情報。競合判定の主基準としては使わない） |

※ `rename` の場合は `old_path` / `new_path` を追加で持たせる。

#### 3.2.1 アプリケーションイベントログ
3.2節のログはVault内容の差分（ファイル変更）を記録するものだが、それとは別に、**役割（Primary/Secondary）のライフサイクルに関する操作ログ**も必要になる（3.6.1.4節の多重Primary衝突検出・解決、3.6.1.5節のStandalone / Syncingステータス判定で使用）。データ量は小さく頻度も低いため、3.2節と同じ「各デバイスは自分のログにしか書き込まない」方式をそのまま踏襲する。

- 配置：`Vault/_sync/events-<device-uuid>.jsonl`（3.2節のデバイスIDをそのまま使う）
- 形式：1行1エントリのJSON Lines（追記のみ）
- 記録するイベント：`initialized_as_primary` / `initialized_as_secondary` / `promoted_to_primary` / `conflict_detected` / `demoted_to_secondary` / `conflict_resolved` / `device_decommissioned`（3.6.1.5節。Reset Configuration実行直前に、そのデバイス自身が記録する離脱シグナル）
- 保持期間：デフォルト365日。各デバイスが自分のサイクルの中で、自分のログファイルのみを対象に古いエントリを間引く（他デバイスのログには一切書き込まない）。これは3.4節で将来タスクとしているログ圧縮の一部を、このイベントログに限り先行実装したもの（3.2節のコンテンツ差分ログ自体の圧縮は、マージの`base_hash`履歴と絡むため引き続きTodoとする）。

### 3.3 マージエンジン
**プライマリノード**（基本はMac、3.6節参照）上でのみ動作する定期実行プログラム（Mac: cron / launchd、Windows: タスクスケジューラ）。Go言語でMac用・Windows用バイナリをそれぞれクロスコンパイルし、単一の実行ファイルとして配布する（ランタイムのインストール不要）。

#### 3.3.0 変更検出方式：ハッシュベース（OS標準のファイル監視機能は不採用）
本エンジンはcron/タスクスケジューラによる**定期実行**を前提とする（常駐デーモンではない）ため、Mac のFSEventsやWindowsのReadDirectoryChangesW等、常時起動を前提とするOS標準のファイル監視機能は採用しない。実行していない間（スリープ中・未起動時）に発生した変更を取りこぼしてしまい、結局ハッシュ比較による補完が必須になるため、二重実装のコストに見合わない。

**変更検出のロジック（Mac/Windows共通）：**
1. 定期スキャン時に、Vault内の全ファイルのパスと内容ハッシュを算出し、前回スキャン時の記録と比較する。
2. 前回存在したパスが消え、かつ新たに現れたパスと**内容ハッシュが完全一致**する場合 → `action: "rename"` として記録（`old_path` / `new_path` を付与）。
3. 上記に該当しない消失・出現は、それぞれ単純な `delete` / `create` として記録する（リネームと同時に内容も変更された場合は区別できず delete+create 扱いになるが、実用上許容する）。
4. 内容ハッシュが変化したパスは `modify` として記録する。

**処理フロー（N台のデバイスに対応）：**
1. 存在する全ログファイル（デバイス数に依存しない）から、ファイルパスごとの最新エントリを抽出する。
2. 対象ファイルについて、変更のあった各デバイスの最新エントリの `base_hash` を比較する。
   - **一致する場合**：共通の分岐点から複数デバイスが変更したとみなし、その`base_hash`に対応する内容を3.4節のスナップショット機構から取得し、実際の分岐点内容としてbaseに使う3-way mergeを実施する（baseを空文字列のまま渡すと、重ならない箇所の変更であっても`git merge-file`が常に全文コンフリクト扱いにしてしまうため、必ず実際の分岐点内容を渡す必要がある）。
   - **一致しない場合**：どちらかが古い版を基準にしている可能性がある。この場合は無理にbaseを揃えようとせず、後述のGUIによる競合解決（該当ファイルの各デバイス版を提示してユーザーに選ばせる）に委ねる。
3. 3台以上が同じファイルを変更している場合は、**3-way mergeを順に繰り返し適用**する（`git`のoctopus mergeと同様の考え方）。
   - 例：デバイスA・B・Cがそれぞれ`foo.md`を編集していた場合、まずbase版とA版・B版を`git merge-file`でマージし、その結果を新たな基準にしてC版をさらにマージする。
   - `git merge-file <A版> <base版> <B版>` を1組ずつ呼び出すだけでよく、N台対応のための特別な実装は不要。
4. マージ結果にコンフリクトマーカー（`<<<<<<<` 等）が含まれない → 自動マージ成功。Vault現物ファイルへ反映し、新たなログエントリとして記録して次回以降の比較の基準とする。
5. コンフリクトマーカーが含まれる（＝重なった箇所を複数デバイスが変更していた、真の競合） → コンフリクトマーカー入りの内容は一旦Vault現物ファイルへそのまま反映する（Obsidian側で開けば内容が見えるようにするため）が、**この時点ではログに「解決済み」として記録しない**。ローカルに「未解決の競合」として保持し、Settings画面から解決できるようにする（8.3節・3.3.2節）。ユーザーはそこでいずれかのデバイス側の内容を採用するか、Obsidian上で直接手動編集して確定できる（手動編集した場合、次回スキャンでその変更が新たな確定版として自然に検出される）。

**競合判定ルール（重要）：**
- 「タイムスタンプが新しい方を優先」というルールは採用しない（オフライン編集による逆転が起こり得るため）。
- 1台のみが分岐点から変更している場合は、自動的にその変更を採用する。
- 2台以上が分岐点から変更している場合のみ、真の競合として扱い、ユーザーに確認する。

#### 3.3.1 なぜ「複数人での同時編集」を想定しない前提でも3-way mergeが必要か（将来の実装者・レビュアー向け）
このアプリは**1人のユーザーが複数端末を使う**ことを前提としており（本節冒頭）、複数の人間が同時に同じVaultへ書き込むことは想定していない。そのため一見、「同時編集がないなら3-way mergeもgit依存も不要では」と思えるかもしれないが、**それでも3-way mergeは必要**であり、削除すべきではない。理由は次の通り：

- **単一ユーザーでも「オフライン編集の分岐」は日常的に起こる。** 例：飛行機移動中にiPhoneでオフライン編集し、まだiCloud同期が完了していない状態で、着陸後すぐにMacでも同じファイルの別の箇所を編集してしまう、というのは特殊なケースではなく普通に起こり得る。
- **この場合、2台のデバイスは同じ分岐点（base）から、互いの変更を知らずに、別々の箇所を編集している。** 正しくbaseを復元した3-way mergeであれば、こうした**重ならない箇所への変更は自動的に両方反映**でき、ユーザーは何も操作せずに済む。
- 3-way mergeをやめて「複数デバイスが触っていたら常にユーザーに選ばせる」方式にすると、このごく普通のケースでも毎回手動対応を強いられ、かつ選ばなかった方の内容（ユーザー自身の正当な入力）が失われるリスクが生まれる。これは自動マージより明確に悪いユーザー体験である。
- 本当にユーザーへ確認が必要なのは、**同じ箇所を複数デバイスが分岐点から変更していた場合（真の競合）のみ**であり、3-way mergeはこの「本当に確認が必要な場合」を正しく絞り込むためにこそ必要になる。
- なお、この仕組みは「1人のユーザーの複数端末」に限定した実装にはなっていない。将来、AI Agent等がこのVaultへ複数端末（あるいは複数プロセス）から並行して書き込むようになった場合にも、同じ仕組みがそのまま活用できる。

#### 3.3.2 真の競合（コンフリクトマーカー）のGUIでの解決フロー
3.3節ステップ5で「真の競合」と判定されたファイルは、以下の流れで解決する。CLIでの対話プロンプト（標準入力からの選択）は採用しない。GUI常駐アプリ（タスクトレイ／メニューバー）・常駐デーモンいずれの実行形態でも、対話的な標準入力を持たない状態で動作するのが通常であり、対話プロンプトを前提にすると実運用ではまず届かない（無反応のままコンフリクトマーカーが放置される）ため。

- **検出・記録**：マージ処理はPrimaryノードでのみ実行される（3.3節冒頭）ため、未解決の競合はPrimary上にのみローカルに保持すればよい（他デバイスと共有・同期する必要はない）。検出した時点の各デバイスのバージョン（`device_id`・`label`・内容）をローカルの`pending_conflicts.json`（6.4節）に保持する。
- **GUIへの可視化**：未解決の競合が1件以上ある間、Settings画面のStatusセクションに警告行（`⚠ N unresolved conflict(s)`）と「Resolve Conflicts...」ボタンが表示される（8.3節）。タスクトレイ／メニューバーのステータス表示も`Status: Conflict`に変化する（3.5.2節）。
- **解決**：「Resolve Conflicts...」を押すと、未解決の競合を1件ずつ、関与した各デバイスのバージョンを選択肢として提示する（3台以上絡む場合は候補をすべて提示）。いずれかを選ぶと、そのデバイスの実際の内容をVault現物ファイルへ書き込み、新たなログエントリとして記録する（3.3節ステップ4と同じ扱いになり、次回以降の比較の基準となる）。
- **手動解決との連携**：ユーザーがダイアログを使わず、Obsidian上で直接ファイルを開いてコンフリクトマーカーを手動で編集・削除した場合も対応する。次回のPrimaryの定期スキャンで、その手動編集がハッシュの変化として検出され、通常の変更として扱われる。この場合、該当する`pending_conflicts.json`のエントリは（記録時点のハッシュと現物ファイルの現在のハッシュが一致しなくなるため）自動的に取り除かれる。誤って古い競合を延々と警告し続けることがないようにするため。

### 3.4 スナップショット管理
`git merge-file`による3-way mergeが正しく機能するには、分岐点（base）の**実際の内容**が必要になる（3.3節）。これを可能にするため、各ログエントリの`diff`フィールド（3.2節のスキーマ）には、**その時点でのファイル内容そのもの**を保存する（初期バージョンでは、真のunified diff形式ではなく、実装を単純化するためファイル全文を保存する簡略実装とする。詳細下記）。

- baseの復元：あるパスについて複数デバイスの最新エントリの`base_hash`が一致する場合、いずれかのデバイスのログ（自分のものとは限らない。3.2節の通り全デバイスのログは読み取り自由）から`result_hash`が一致するエントリを探し、その`diff`フィールドの内容をbaseとして使う。
- 見つからない場合（該当エントリが既に圧縮・削除されている、または元々記録されていない古いデータ等）は、無理にbaseを推測せず、3.3節ステップ5の「真の競合」として扱いGUIでの解決に委ねる。安全側に倒す。
- **将来のTodo（圧縮）：** diffの連鎖が長くなり、ログ肥大化・ストレージ増加につながるため、将来的に定期圧縮の仕組みを導入する（**初期バージョンでは圧縮なしとし、Todoとする**。実装方針の詳細は5節参照）。圧縮を実装する際は「それ以前の古いエントリを削除し、直近の`base_hash`をVault現物ファイルの現在のハッシュに更新する」形をとる想定。圧縮によって古いエントリの内容（＝上記のbase復元に使う情報）が失われるため、**圧縮対象からは「まだ他デバイスとのbase_hashの分岐点として参照され得る直近のエントリ」を除外する**よう注意が必要（詳細な閾値は実装時に検討）。
- **将来のTodo（真のunified diff化）：** 現状はファイル全文を保存する簡略実装のため、編集回数の多いファイルではログサイズが線形に増加する。将来的に真のunified diff形式（3.2節のスキーマが元々想定していた形）へ移行し、保存量を差分サイズのみに抑えることを検討する。

### 3.4.1 実行間隔・変更確定タイミング（デバウンス方式）
iCloud（および iCloud for Windows）には「同期完了」を外部から確実に検知できる公式な仕組みが無い。内部APIを使う方法もあるが非公式であり、将来の変更で壊れるリスクを新たに持ち込むことになるため採用しない。またGoogle Drive公式アプリは不採用（2章参照）のため、そちらの同期タイミングに合わせる必要もない。

**採用方式：変化が止まるまで待つ「デバウンス」方式**
1. プライマリノードはデフォルト**600秒（10分）**間隔でVaultを定期スキャンする（Settings > rclone > Advanced Optionsで変更可能。旧デフォルトは120秒だったが、以下の理由で緩和した）。
2. あるファイルの内容ハッシュが**2回連続のスキャンで変化していない**場合にのみ「安定した」とみなし、そのファイルをログ記録・マージ対象に含める。
3. 直近のスキャンでハッシュが変化し続けているファイルは「まだ同期中／編集中」とみなし、その回の処理対象からは除外し、次回以降に持ち越す。

この方式はOS・同期アプリの内部実装に依存せず、Mac/Windows共通のロジックで実現できる。

**間隔のデフォルトを600秒に緩和した理由：** この間隔が制御するのはGoogle Driveバックアップの反映頻度であり、端末間の同期自体（iCloud経由、1.3節）には一切影響しない。Google Driveは「人間が直接編集しない、読み取り専用に近いバックアップ先」（3.5節）であり、リアルタイム性より低頻度なバックアップの方が実用上十分である。加えて、3.6.1.4節で追加した「毎サイクルのPrimaryマーカー再確認」処理はサイクルごとに小さなGoogle Drive API呼び出しを伴うため、間隔を伸ばすことでその負荷も抑えられる。

#### 3.4.2 変更確定の基準となる状態は、デバウンス比較用の状態と分離して保持する（重要）

デバウンス処理（3.4.1節）とログへの変更記録（3.2節）を素朴に同じ1つの保存状態（`_sync/state/last_scan.json`）だけで実装すると、**ファイルの編集が正しくログに記録されない**という不具合が発生する（実際に過去のバージョンで発生していた）。

**不具合の内容：** 「前回のスキャン結果」を、(a) デバウンス判定（2回連続で同じハッシュか）の比較対象と、(b) 「何が変わったか」を確定するログ記録の比較対象の、両方に兼用してしまうと、ファイルを編集した直後のサイクルでは「デバウンス未確定＝安定版に含まれていない」状態を「前回はあったのに今は無い＝削除された」と誤認識し、偽の`delete`がログに記録される。さらにその次のサイクルでは、既に「前回の状態」として新しい内容が（生スキャンとして）記録されてしまっているため、今度は「差分なし」と判定され、本来のcreate/modifyは一度もログに残らない。結果として、既存ファイルへの編集は、実運用では正しく追跡されないことになる。

**対策：状態を2つに分離して保持する。**
- **`_sync/state/last_scan.json`**：デバウンス比較専用。純粋に「前回の生スキャン結果」であり、確定・ログ記録とは無関係。毎サイクル、生のスキャン結果でそのまま上書きする（この点は変更前と同じ）。
- **`_sync/state/last_confirmed_scan.json`**（新設）：ログへの変更記録（`DetectChanges`）が比較対象とする「確定済みの状態」。**このサイクルで実際にログへ記録した変更の分だけを反映して更新する**（`ApplyChangesToState`）。まだデバウンス未確定（安定していない）パスは、たとえ内容が変わっていても一切触らず、直前の確定値をそのまま残す。

**さらに、「未確定」と「本当の削除」を区別する必要がある。** 単純に「安定版に含まれていない＝削除」と判定すると、編集直後でまだ2回連続の安定を満たしていないファイルも削除扱いになってしまう（対策の分離だけでは解決しない）。そのため、変更検出の直前に、生スキャン結果も参照して次のように補正した状態（`ReconcileForDetection`）を作り、それを`DetectChanges`の比較対象にする：
- パスが安定版に含まれる → その新しい値を採用する（＝create/modifyとして検出される）。
- パスが安定版には無いが、生スキャン結果にはまだ存在する → **まだ変化の途中**なので、削除とは見なさず、直前の確定値のまま据え置く（このサイクルでは差分なしとして扱われる）。
- パスが生スキャン結果にも存在しない → 本当に削除されたと判定する。

この結果、ファイルを1回編集すると、①デバウンスが安定するまでの1〜数サイクルは差分なし、②安定した時点で`modify`として1回だけ正しく記録される、という期待通りの挙動になる（偽の`delete`は発生しない）。

この仕組みはVault本体のスキャン（3.3節）・iCloud Bridgeフォルダのスキャン（1.6.3節）の両方で共通して使用する。

### 3.5 Google Driveへのミラー転送

> **注：本節はPrimaryの一方向ミラー（publish）についてのみ記す、部分的に古い内容。** 1.6.4節の通りSecondaryもGoogle Driveへ接続するようになっており（実装済み）、全体像は1.6.4節を参照。本節はPrimaryのpublish部分の詳細（リトライ方針等）としては引き続き有効。

- ツール：rclone（`rclone sync`）
- 実行タイミング：マージエンジンの処理が完了し、Vault現物ファイルとログの整合性が取れた後
- 転送方向：プライマリノード → Google Drive の一方向のみ
- 認証：`rclone config` 実行時のブラウザOAuth同意フローで設定（APIキーの手動発行・貼り付けは不要。デバイスごとに個別設定でよい）
- 対象：Vault現物ファイル一式 ＋ `_sync/` ログディレクトリ一式

#### 3.5.0.1 バックアップ先フォルダ名とVault切り替え時の安全性
`rclone sync` は同期先を同期元と完全一致させる**ミラー転送**であるため、同期元に存在しないファイルは同期先から削除される（2章）。もしGoogle Drive Target Folder Path（`rclone_path`）が固定文字列のままVaultを別のフォルダに変更すると、次回の同期で**旧Vaultのバックアップファイルが新Vaultの内容で上書き・削除されてしまう**。

これを防ぐため、Google Drive Target Folder Pathは**選択したVaultフォルダ名を初期値として自動提案**する（例：Vaultフォルダ名が`MyVault`なら提案値も`MyVault`）。ユーザーが値を変更していない限り、Vaultフォルダを選び直すたびにこの提案値も追従して更新される。一方、ユーザーが明示的にカスタム値へ変更した場合は、以降Vaultを変更してもその値を保持する（自動上書きしない）。

加えて、Save Settings実行時に「保存済みのVaultパスと異なるVaultへ変更されているのに、Google Drive Target Folder Pathが前回保存時から変わっていない」場合は、上記の事故が起こり得る旨を警告し、確認を求める（3.5.2節）。

さらに踏み込んで、**rcloneリモートが設定済みの状態でのVaultフォルダ変更そのものを禁止する**（3.5.3節）。リモートが設定済みということは、そのリモートが現在のVaultを実際にバックアップし得る状態にあるということであり、Vaultだけを変更してリモートをそのまま残すと、次回同期時に新しいVaultの内容が古いVaultのGoogle Drive保存先へ気づかないうちにバックアップされてしまう。この操作を行うには、先に「Remove Remote Configuration...」でリモート設定を削除し、Vaultフォルダを変更した後、改めてリモートを設定し直す必要がある。

不要になった過去のバックアップは、Google Drive上で手動削除すればよい（アプリ側から自動削除は行わない）。

#### 3.5.1 失敗時のリトライ・アラート方針
- `rclone sync` が失敗した場合、**指数バックオフで再試行**する（例：30秒後→2分後→10分後の計3回）。
- 3回とも失敗した場合はその回の転送をスキップし、失敗内容をローカルにログとして記録した上で、**次回の定期実行（3.4.1節のデバウンス済みの回）で改めて実行を試みる**。無限リトライで処理が詰まらないようにする。
- 通知は3.5.2節の方針に準じる（初期はログ出力のみ、将来デスクトップ通知に拡張）。

#### 3.5.2 コンフリクト通知・処理結果およびGUI UI仕様
- **起動時・Settings起動フロー**:
  - `Settings...` または未初期化時の設定画面表示は、**Git や rclone のインストール状態にかかわらず即座に単一GUIウィンドウとして起動**する。表示前のダイアログブロックやフリーズを徹底排除する。
  - rclone や Git が未検出の場合は、設定画面の **Status セクション** に `Not Found` と表示され、画面内のアクションボタンからダウンロードやセットアップを安全に起動できる。
- **Settings（設定画面）機能とレイアウト仕様**:
  - **単一統合 GUI ウィンドウ**: ステップごとの個別プロンプトではなく、1つのウィンドウ内にステータス・設定フォーム・rclone情報を視覚的に整理して表示する。
  - **[ Status セクション ]**:
    - Git インストール状態 (`Git status: Installed / Not Found`)
    - rclone インストール状態 (`rclone status: Installed / Not Found`)
    - iCloud for Windows インストール状態（Windowsのみ表示。`Install iCloud...` ボタンで自動インストール、3.6.4節）
    - Google Driveの同期状況（`Last synced: <日時>` / `Last sync failed (<日時>): <エラー>` / 未実行なら `Never synced yet`。Secondary機では実行主体でないことを示す固定文言を表示。ボタンは無し、直近の`rclone sync`結果を表示するのみ）
    - デバイス役割 (`Device Role: Primary / Secondary`)。Secondaryのとき、または多重Primary衝突が発生しているときは、この行に`Promote to Primary...`ボタンが表示される（3.6.1.2節・3.6.1.4節）。役割が確定していれば常に、`Primary (Standalone)` / `Primary (Syncing)` / `Secondary (Syncing)`のようにStandalone / Syncingステータス（3.6.1.5節）が付記される
    - 多重Primary衝突が発生している場合のみ追加表示される警告行（`⚠ Primary conflict: ...`）: 相手デバイスのラベルと状況の説明（3.6.1.4節）
    - 未解決のコンテンツ競合（3.3.2節）が1件以上ある場合のみ追加表示される警告行（`⚠ N unresolved conflict(s)`）と「Resolve Conflicts...」ボタン。Primaryのみが持ち得る状態（マージ処理はPrimary上でのみ実行されるため）
  - **[ Obsidian Vault セクション ]**:
    - **Vault Folder Location**: テキスト入力 ＋ `[ Select Folder... ]` ボタン。ラベルや案内文には「Directory」「Path」等の専門用語を避け、一般ユーザーにもわかりやすい「Folder」表記に統一する。`[ Select Folder... ]` は実行中のOSが提供する標準のフォルダ選択ダイアログ（macOS: Cocoaのフォルダ選択パネル、Windows: `IFileDialog`）を表示する。SettingsウィンドウはFyne製だが、フォルダ選択だけはFyne独自のダイアログ（見た目がOS標準と異なり違和感を与える）ではなく、OS標準の見た目のダイアログを使う。iPhone/Windows間の同期はiCloudに委任するため（1.3節）、このセクションに同期間隔などの設定項目は置かない。**既にVaultが設定済みで、かつrcloneリモートが設定済みの場合、テキスト入力・`[ Select Folder... ]`ボタンとも非アクティブ化し**、「先にGoogle Driveリモートを削除してください」という案内文を添える（3.5.0.1節・3.5.3節・3.6.1.5節）。
  - **[ rclone セクション ]**: Google Driveへのバックアップに関する設定は、それが有効になる（rcloneの設定が完了する）までは意味を持たないため、すべてこのセクションにまとめる。
    - **Remote Status**: rcloneリモートの設定状態 (`Configured` / `Not Configured` )
    - **Executable**: 検出・利用中の `rclone` バイナリの実行パス
    - **[ Advanced Options ]（折りたたみ表示、デフォルトで閉じた状態）**: 以下の3項目は、いずれもデフォルト値のまま動作する「通常は触る必要のない」設定であるため、`widget.Accordion`で折りたたみ、初期表示では隠す。展開すれば通常通り編集できる。
      - **Remote Name**: 設定されているリモート名（デフォルト `ObsidianVault`）
      - **Google Drive Target Folder Path**: 転送先フォルダパス。**デフォルトは固定文字列ではなく、選択したVaultフォルダ名を自動提案する**（3.5.0.1節）。Vaultフォルダを選び直すたびに追従するが、ユーザーが手動で変更した値は上書きしない。
      - **Sync Interval**: プライマリノードがVaultをスキャンし、変更をマージしてGoogle Driveへバックアップする周期（秒単位、デフォルト `600` 秒。3.4.1節）。iCloud経由の端末間同期はOS側が自動で行うため、この間隔設定はGoogle Driveへのバックアップ頻度にのみ影響する。
    - **Configure Google Drive Remote... ボタン**: Save Settingsを押す前でも、その場でrcloneのGoogle Drive認証（新規セットアップ／既存・CLI設定）を実行できる。
    - **Remove Remote Configuration... ボタン**: リモートが設定済みの場合のみ表示。確認ダイアログの後、`rclone config delete` でそのリモートのGoogle Drive認証情報を削除する（Google Drive上のバックアップ済みファイル自体は削除されない）。別のGoogleアカウントで設定し直したい場合などに使用する。以前はこの操作に対応するGUI動線が存在せず、Terminal/PowerShellでの手動操作が必要だった。
    - **Promote to Primary... ボタン**（Statusセクション、Device roleの行）: このデバイスがSecondaryのとき、または多重Primary衝突が発生しているときに表示される（3.6.1.2節・3.6.1.4節）。確認ダイアログの後、このデバイスをPrimaryとして`PRIMARY_MARKER.json`に書き込む。衝突が発生していた場合は、併せて`PRIMARY_CONFLICT.json`を解消する。
  - **操作ボタン**:
    - **Save Settings ボタン**: 全設定を一括保存し、自動的に `init` 処理（初回セットアップ・役割判定）を実行して同期ループへ反映する。前回保存時からVaultパスが変更されているのにGoogle Drive Target Folder Pathが変わっていない場合は、保存前に警告・確認ダイアログを表示する（3.5.0.1節）。
    - **Cancel ボタン**: 変更を行わずに設定画面を閉じる
    - **Reset Configuration ボタン**: 設定およびローカルの役割情報をクリアし、初期設定状態へ戻す（誤設定時やVault再設定時に使用）。**Settings画面内のボタンとしてのみ提供し、メニューバー／タスクトレイのメニュー項目には配置しない**（誤操作防止のため、Settings画面を開いた上で明示的に実行する動線に統一する）。
    - **ボタンの色分け方針**: 常時画面に表示されているボタン（Reset Configuration・Remove Remote Configuration...）は、それ自体は破壊的操作を実行しない（確認ダイアログを開くだけ）ため、警告色（赤）ではなく落ち着いた配色にする。赤色は、実際にその操作を確定させる確認ダイアログ側のボタン（例:「Are you sure?」の「Yes」）にのみ使う。画面内で最も視覚的に目立たせるのは **Save Settings ボタン**（強調色）とする。
    - **未保存の変更インジケーター**: Vault Folder Location・Remote Name・Google Drive Target Folder Path・Sync Intervalのいずれかが、Settings画面を開いた（または最後に再構築した）時点の値と異なる場合、ボタン行に「● Unsaved changes」という強調表示のテキストを表示する。値を元に戻せば表示は消える。Save Settingsボタン自体は常にクリック可能な状態を維持し、このインジケーターの有無によって無効化はしない（保存前のバリデーションはクリック時に行う）。
- **ビジュアルデザイン・アイコン仕様**:
  - モチーフは「循環する2本の矢印」（端末間／Google Drive間でのデータ同期を想起させる図形）。単なる幾何学模様ではなく、同期という機能を直接連想できる図柄を採用する。
  - **アプリアイコン**（Dock・Finder・タスクバー用、`assets/AppIcon.icns`）: 視認性を優先し、**Obsidian テーマカラー（パープル #7C3AED）** の角丸背景に白色の同期矢印を配置する。
  - **macOSメニューバー常駐アイコン**: OS標準のメニューバーアイコンとの親和性を優先し、**パープルを使わないモノクロ図形**（透明背景＋黒のシルエット）を採用する。Fyneの `ThemedResource` としてラップして渡すことで、macOSのテンプレート画像機構により、ライトモード／ダークモードのメニューバー配色に応じてOSが自動的に色を反転してくれる（実装側で個別に白版・黒版を用意する必要がない）。
  - **Windowsタスクトレイ常駐アイコン**: Windowsにはメニューバーのような自動色反転の仕組みがないため、パープル背景＋白矢印の**アプリアイコンをそのまま縮小して使用**する。タスクトレイは非常に小さく表示されるため、モノクロ版よりも彩色されたアプリアイコンの方が背景を問わず視認性を保ちやすい。
- **コンフリクト通知**:
  - 未解決のコンテンツ競合（3.3.2節）・多重Primary衝突（3.6.1.4節）のいずれかがある間、メニューバー／タスクトレイのステータス表示が `Status: Conflict` に変化する。クリックでSettings画面を開き、そこから解決する（8.3節）。
  - CLIモード（`unitevault run`等）を含め、標準入力を使った対話プロンプトは提供しない（3.3.2節：常駐GUI・デーモンいずれも対話的な標準入力を前提にできないため）。CLI実行時も、解決自体はSettings画面から行う想定。

#### 3.5.3 設定変更操作の排他制御・実行順序の安全性
このアプリはVault実体・ローカル同期状態・rcloneリモート設定を扱うため、「同期サイクル実行中にVaultパスを変更する」「rcloneの停止前に別の設定変更をする」「セカンダリのrclone設定より先にプライマリ側を削除する」といった**望ましくない順序での設定変更**は、データ不整合やGoogle Drive上のミラー転送事故につながり得る。これを避けるため、以下2点の排他制御をアプリ内部（1プロセス内）で行う。

- **重複デーモンループの防止**: 従来、Save Settings成功のたびに新しい定期同期ループ（`runDaemonLoop`）を`go`で起動する一方、直前まで動いていたループを止める処理がなかった（アプリ終了時にのみ全体が停止）。そのため、Save Settingsを複数回行うと、スキャン・マージ・rclone同期を行うゴルーチンが際限なく積み重なり、しかもそれぞれが保存当時のVaultパス・設定を保持したまま古い内容で同時並行に動き続けるという潜在的な不具合があった。これを`startDaemonLoop`/`stopDaemonLoop`に一本化し、新しいループを開始する前に必ず直前のループを`context`キャンセルで停止するよう修正した。
- **同期サイクルの多重実行防止と、破壊的操作のガード**: 1台のデバイス内で`SyncEngine.RunCycle`（定期実行のティック・Sync Now共通）が同時に2つ以上走らないよう、`cycleMu`によるノンブロッキングな排他ロック（`tryBeginExclusiveOp`）で直列化する。このロックは以下の**破壊的・順序依存の設定変更操作**の実行条件としても兼用し、同期サイクルが進行中の間はこれらの操作を開始させない：
  - **Save Settings**（Vaultパス・rclone設定の変更を伴う）
  - **Reset Configuration**（設定・役割情報の初期化。合わせて`stopDaemonLoop`でループ自体も停止する）
  - **Configure Google Drive Remote...**（rcloneのGoogle Drive認証セットアップ）
  - **Remove Remote Configuration...**（rcloneリモート設定の削除）
  - **Promote to Primary...**（役割・`PRIMARY_MARKER.json`・`PRIMARY_CONFLICT.json`の書き換えを伴う。3.6.1.2節・3.6.1.4節）
  - ロック取得に失敗した場合（＝いずれかの同期サイクルまたは他の破壊的操作が進行中）は、操作を中断し「Sync In Progress」ダイアログで、完了を待って再試行するよう案内する。待機キューに入れて自動的に後から実行する、という挙動はあえて採らない（ユーザーが気づかないタイミングで意図しない変更が走ることを避けるため）。

セカンダリのrclone設定を先に、プライマリのrclone設定を後に削除するといった**複数デバイスをまたぐ**順序（本節が扱うのは単一デバイス内の排他制御のみ）については、3.6.1.4節のマルチPrimary衝突検知・解決の仕組み（`PRIMARY_MARKER.json` / `PRIMARY_CONFLICT.json`）が実質的にカバーする。PrimaryでのVaultフォルダ変更・Reset Configurationについては3.6.1.5節を参照。

### 3.6 マルチOS・複数台対応

#### 3.6.1 プライマリノードの決定方式：固定ハブ方式（確定）
マージ処理・Google Drive転送を複数デバイスが同時に実行すると競合するため、**プライマリノードを1台に固定する固定ハブ方式を採用する**（動的ロック方式は不採用。参考として3.6.1.3節に理由を残す）。

固定ハブ方式を採用する場合、**プライマリノードが長時間オフラインの間は、末端デバイス側でいくら編集が溜まってもマージ・Google Drive反映が行われない**点に注意する。末端デバイスの台数が増えるほど、プライマリノードをこまめに起動しておく運用の重要性が増す。

##### 3.6.1.1 初回起動時のプライマリ／セカンダリ自動判定
「どちらがプライマリか」をユーザーが手動設定する必要はなく、**Google Drive上に管理用マーカーファイルが存在するかどうかで自動判定する**。

- マーカーファイル：`_sync/PRIMARY_MARKER.json`（プライマリのデバイスID・初期化日時等を記録）をGoogle Driveのバックアップ用フォルダ直下に置く。
- 判定基準は「フォルダが空かどうか」ではなく、**このマーカーファイルの有無**とする。ユーザーが手動で置いた別ファイルの存在や、rclone初回同期が中断して中途半端にファイルが存在する状態を「初期化済み」と誤認しないため。

**`PRIMARY_MARKER.json` のスキーマ：**
```json
{
  "schema_version": 1,
  "primary_device_id": "8f3a1c2e-....-....",
  "primary_label": "mac-mini",
  "initialized_at": "2026-08-23T10:00:00+09:00",
  "vault_root_hash": "abcdef123456...",
  "app_version": "0.1.0"
}
```

| フィールド | 用途 |
|---|---|
| `schema_version` | 将来スキーマを変更する際の互換性判定用 |
| `primary_device_id` | プライマリのデバイスID（レース条件チェックに使用。下記フロー参照） |
| `primary_label` | 人間向け表示名 |
| `initialized_at` | 初期化日時（トラブル時の切り分けに使用） |
| `vault_root_hash` | 初期化時点でのVault全体の代表ハッシュ（全ファイルハッシュのハッシュ等）。将来、破損検知や整合性チェックに使う保険的フィールド |
| `app_version` | 書き込んだエンジンのバージョン。将来のスキーマ変更時の判定に使用 |

**初回起動時の処理フロー：**
1. `rclone`でGoogle Drive上の`_sync/PRIMARY_MARKER.json`の有無を確認する。
2. **マーカーが存在しない場合 → 自分がプライマリノードとして初期化する。**
   - ローカルのVault（現物ファイル）を初期スナップショットとして扱う。
   - 自分のデバイスID（UUID）を生成し、`PRIMARY_MARKER.json`に記録して`rclone`でアップロードする。
   - アップロード直後に**もう一度ダウンロードして内容を確認**し、記録されているデバイスIDが自分自身であることを確かめてから正式にプライマリとして動作を開始する（複数台がほぼ同時に初回起動した場合のレース条件対策。自分以外のIDが書かれていた場合は3の手順でセカンダリとして初期化し直す）。
3. **マーカーが存在する場合 → 自分はセカンダリ（末端デバイス）として初期化する。**
   - 自分専用のデバイスID（UUID）を生成し、空のログファイル（`log-<自分のUUID>.jsonl`）を作成する。
   - 以降はこのVaultフォルダ（iCloud経由で既に取得済みの内容）を起点に、通常の編集・自分のログへの追記のみを行う（マージ処理・Google Driveへの書き込みは一切行わない）。
   - **Google Drive上のVault・ログ一式を`rclone copy`でダウンロードする処理は行わない。** 端末間のVault本体の配布は常にiCloud Driveが担っており（1.3節）、セカンダリの同期サイクル自身も他デバイスのログを一切参照しない（参照するのはプライマリのマージフェーズのみ）ため、この`rclone copy`はセカンダリの動作上一度も必要にならない。過去のバージョンでは「念のため」実行していたが、これはiCloudが既に配置済みのVaultフォルダにGoogle Drive側の内容を重ねて書き込む形になり、Windows環境で実際にファイル競合を引き起こす不具合の原因になっていた。

##### 3.6.1.2 プライマリノードの移行（手動昇格）
Macが故障・引退する等でプライマリを他デバイスに引き継ぎたい場合、Settings画面のStatusセクションから**「Promote to Primary...」ボタン**で昇格できる（Secondary機のDevice role行に表示される）。

- 昇格処理は、新しくPrimaryにしたいデバイスで`PRIMARY_MARKER.json`に自分のデバイス情報を書き込み、Google Driveへアップロードするだけでよい（`rclone`の`copyto`は上書き転送のため、**旧マーカーを事前に削除する手順は不要**）。
- 旧プライマリの扱いについては3.6.1.4節を参照。旧プライマリが完全に故障している場合はそのまま放置してよいが、旧プライマリが一時的にオフラインだっただけで後日オンラインに復帰する可能性がある場合は、3.6.1.4節の多重Primary衝突検出の仕組みにより自動的に安全な状態へ収束する。

##### 3.6.1.3 （参考）動的ロック方式を不採用とした理由
`_sync/engine.lock`（デバイスID・取得時刻を記載）をVault内に置き、早い者勝ちでプライマリ役を起動する方式も検討したが、以下の理由で不採用とした。

- iCloudの同期反映にはラグがあるため、ごく短時間に複数デバイスが同時にロックを取得してしまう可能性がある。
- 個人利用では「常時複数デバイスが同時に処理を試みる」状況自体が稀であり、固定ハブ方式の単純さに対するメリットが小さい。

##### 3.6.1.4 Primary役の継続的検証と多重Primary衝突の検出・解決

**背景・課題：** 3.6.1.1節の役割判定は初回起動時（正確には、ローカルにキャッシュされた役割が無い場合）にのみ行われ、一度Primaryと判定されたデバイスは、以降そのキャッシュ（ローカルの`role`ファイル）を無条件に信頼し続ける。しかし3.6.1.2節のようにSecondary機がPromote to Primaryで昇格すると、**旧Primary機は自分が降格したことを知る手段がなく、動作し続けている限り両方のデバイスが「自分こそPrimaryだ」と信じてマージ処理・Google Drive転送を並行して実行してしまう**（いわゆるスプリットブレイン）。この状態でGoogle Drive同期が2台から同時に走ると、Google Drive上のバックアップ内容が競合・破損しかねない。

**採用方式：毎サイクルのマーカー再確認＋多重Primary衝突の検出・一時停止・人手による解決**

1. **毎サイクルの再確認**：Primaryとしてマージ・Google Drive同期を実行する**直前**に、必ずGoogle Drive上の`PRIMARY_MARKER.json`を再ダウンロードし、`primary_device_id`が自分自身のままかを確認する（3.6.1.1節の初回判定とは別に、サイクルごとに行う）。一致しなければ、それ以降の処理を一切行わずそのサイクルを終える。これにより、2台が同時にGoogle Driveへ書き込むこと自体をほぼ確実に防止する。
2. **衝突の記録**：上記の再確認で「自分が既にPrimaryだと思っていたのに、マーカーが別のデバイスを指している」ことを検出した場合、単純に黙って降格するのではなく、`_sync/PRIMARY_CONFLICT.json`という共有ファイルをGoogle Drive上に作成する（存在しない場合のみ。既に他デバイスが作成済みならそれを採用する）。このファイルには「降格されたデバイス」と「マーカー上の現Primary」双方のデバイスID・ラベルを記録する。
3. **双方の同期停止**：`PRIMARY_CONFLICT.json`が存在する間は、**どちらの側のデバイスもGoogle Drive同期を一時停止する**。マーカー上のPrimaryとして認識されている側のデバイスも、次のサイクルで`PRIMARY_CONFLICT.json`の存在を検知したら同様に同期を停止する（自分が「正しい」側だと思っていても、人手による解決が済むまでは同期しない）。早期に問題を解決する動機付けにもなる。
4. **GUIへの可視化**：`PRIMARY_CONFLICT.json`が存在する間、影響を受けている側のSettings画面のStatusセクションに警告行（`⚠ Primary conflict: ...`）と「Promote to Primary...」ボタンが表示される（8.3節）。降格された側・現Primary側のどちらのデバイスでもこのボタンを押すことができ、それが実質的な「Authorize（承認）」操作になる。
5. **人手による解決（Authorize）**：いずれかのデバイスで「Promote to Primary...」を実行すると、そのデバイスを正式なPrimaryとして`PRIMARY_MARKER.json`に書き込み、`PRIMARY_CONFLICT.json`を削除する（3.6.1.2節の昇格処理と全く同じ実装を再利用する）。
6. **もう一方の自動収束**：解決されなかった側のデバイスは、次回のサイクルで「マーカーは依然として自分を指していないが、`PRIMARY_CONFLICT.json`は消えている」ことを検知し、その時点で初めて正式にSecondaryへ降格する（ローカルの`role`ファイルを更新）。つまり、**旧Primary機が長期間オフラインで、後日オンライン復帰した場合でも、すぐにGoogle Drive同期を再開せず、まずこの手順で安全性を確認してから収束する**。

**アプリケーションイベントログとの関係：** 上記の`conflict_detected` / `demoted_to_secondary` / `conflict_resolved` / `promoted_to_primary`は、3.2.1節のアプリケーションイベントログに記録され、後から経緯を追跡できる。

**設計判断の要点：** 降格するかどうかの安全性に関わる判定は、常に`PRIMARY_MARKER.json`の内容のみを根拠とし、イベントログの有無では判定しない。Google Drive同期の反映には遅延があり得るため、「イベントログが見つかったら初めて降格する」という設計にすると、ログの反映が遅れている間は旧Primaryが誤って同期を継続してしまう可能性がある。イベントログはあくまで人間向けの説明・監査証跡としてのみ用いる。

##### 3.6.1.5 Standalone / Syncingステータスと、Vault変更・Reset Configurationの複数デバイス間での安全性

**背景・課題：** Vaultフォルダは「複数デバイスがiCloud経由で同期する、同一の実体」であることが前提になっている（1.3節）。この前提はデバイスをまたいで共有されているにもかかわらず、Vaultフォルダの変更自体は**各デバイスがそれぞれ単独で、ローカルの設定として行う**操作である。したがって、あるデバイス（特にPrimary）がVaultフォルダを変更しても、他のデバイスには一切伝わらない。他のデバイスは何も知らずに元のフォルダを同期し続け、変更した側だけが別のVaultを見るようになる。Primaryがこれを行うと、共有Vaultへの Google Drive バックアップも同時に止まる（Primaryは変更後の別Vaultをバックアップするようになるため）。同様に、Primaryが Reset Configuration を行うと、他のデバイスが残っていても Google Drive バックアップは即座に停止する（3.6.1.4節の Promote to Primary で復旧は可能）。

これらは「本来あるべき操作順序」の話であり、3.5.3節（同一デバイス内の排他制御）とは性質が異なる：3.5.3節は「同期処理中に設定変更をしてよいか」という**単一デバイス内**の話、本節は「他のデバイスの状況を踏まえてこの操作をしてよいか」という**複数デバイスをまたぐ**話である。

**「他のデバイス」に数えるのはPCのみ：** ここで問題になる「他のデバイス」とは、**このアプリ自身を実行しているPC（Mac／Windows）**を指し、iPhone/iPadは含まない。iPhoneは「編集端末のみ（iCloud同期以外の処理は行わない、追加インストール不要）」（1.4節）であり、このアプリを一切実行しない＝イベントログ・コンテンツ差分ログのいずれも書き込まない。したがって、iPhone/iPadは以下の検知の対象に構造上決して現れない。逆に言えば、**PC 1台のみ＋iPhone/iPad任意台数という構成は「Standalone」であり、PCが2台以上（Primary＋Secondaryが最低1台）揃って初めて「Syncing」になる**。Syncingとは、複数のPCがGoogle Drive同期でエラーを起こさないよう互いに協調し合っている状態を指す（3.6.1.1節・3.6.1.4節の固定ハブ方式・多重Primary衝突検出の対象そのもの）。iPhone/iPad間の同期はiCloudが担うため、このアプリの協調機構には一切関与しない。

**検知手段とその限界：** このアプリには、他のPCが「今も現役で使われているか」を確実に知る手段がない。分かるのは、Vaultの `_sync/` に他デバイスのイベントログ（`events-<uuid>.jsonl`、3.2.1節）が存在するかどうかだけであり、これは一度でも参加したデバイスがあれば残り続ける（自動的には消えない）。この限界を補うため、**明示的な離脱シグナル**を導入する：

- **Reset Configuration を実行する直前**に、そのデバイス自身のイベントログへ新しいイベント種別 `device_decommissioned`（`EventDeviceDecommissioned`）を記録してから、ローカルの設定・役割をクリアする。これにより、「単に長期間オフラインなだけ」と「意図的にこのVaultから離脱した」を区別できるようになる。
- 各デバイスについて「最後に記録されたイベント」を見て、それが `device_decommissioned` であれば、そのデバイスはもう参加していないものとして扱う（`eventlog.Manager.LatestEventForEachDevice`）。同じデバイスIDが後日また初期化されれば、新しいイベントがそれを上書きする。
- この判定はあくまでヒューリスティックである。デバイスが壊れた・紛失した等でReset Configurationを一度も実行できなかった場合は、そのデバイスは離脱後も「現役」として扱われ続ける。誤検知（実際にはもう使われていないPCを「まだ使われている」と判定する）はあり得る前提で、次の通り**警告に留め、操作を強制的にブロックはしない**。

**Standalone / Syncingステータス：** 上記の判定により、Primaryが「他に現役のPCが1台もない」と判断できる状態を **Standalone** と呼ぶ（一度も他PCと組んだことがない場合と、過去にいたSecondaryが全てReset Configuration経由で明示的に離脱した場合の両方を含む）。他に現役のPCが1台以上ある状態を **Syncing** と呼ぶ。Settings画面のStatusセクションで、Device roleの表示に `Primary (Standalone)` / `Primary (Syncing)` のように付記される（8.3節）。Standaloneの間は、以下の警告は表示されない（他に影響を受けるPCがいないため）。Secondaryは常に「どこかにPrimaryがいる」ことを前提とするため、常に`Secondary (Syncing)`となり、Standaloneにはなり得ない。

**警告を表示する2つの操作（いずれもPrimaryかつ他に現役のPCがある＝Syncing状態の場合のみ）：**

1. **Save Settingsでの Vault フォルダ変更**：確認ダイアログで、他のデバイスには影響しないこと・このデバイスがGoogle Driveバックアップを行わなくなることを説明し、続行するか確認する。
2. **Reset Configuration**：確認ダイアログで、他のデバイスが引き継ぐまでGoogle Driveバックアップが止まることを説明し、続行するか確認する。

いずれも `gui.ConfirmDanger` による確認ダイアログであり、ハードブロックはしない（検知がヒューリスティックであるため）。

**GUI上でのボタンの非アクティブ化：** 3.5.0.1節・3.5.3節の「rcloneリモートが設定済みの間はVaultフォルダを変更できない」制約は、こちらとは異なり**確実に判定できる**（rclone側の状態を直接問い合わせられるため）。そのため、この制約についてはSave Settingsを押してから警告を出すのではなく、Settings画面の「Vault Folder Location」欄と「Select Folder...」ボタン自体をあらかじめ非アクティブ化し、「先にリモートを削除してください」という案内を添える（8.3節）。一方、本節のSyncingステータスに基づく警告はヒューリスティックであるため、同様にボタンを非アクティブ化することはしない（誤検知時にユーザーが正当な操作すらできなくなってしまうため）。**判定を確実に行えるかどうかが、事前のボタン非アクティブ化と事後の確認ダイアログのどちらを選ぶかの基準になる。**

**複数デバイスをまたぐ推奨ワークフロー：** 設定を作り直す・デバイス構成を変更する場合は、**Secondary側から先にReset Configurationを行い、最後にPrimaryを片付ける**ことを推奨する。この順序を守れば、Primary側がReset Configurationを行う頃には全SecondaryのPCが離脱済みとなり、Standaloneとして警告なく実行できる。逆の順序（Primaryを先に片付ける）でも、3.6.1.4節のPromote to Primaryにより復旧は可能だが、いずれかのSecondaryが一時的にGoogle Driveバックアップの空白期間を経験することになる。この推奨順序は、1台のアプリだけでは強制できない（他デバイスの状態を直接観測できないため）ため、運用上のガイドラインとして明文化するに留める。

##### 3.6.1.6 iCloudの既知の制約とアーキテクチャ移行の経緯

**iCloud Driveのコンフリクト処理（調査結果）：** iCloud Driveの「Documentsスコープ」（Finder／Filesアプリで見える通常のフォルダ階層。Vaultが置かれる場所）にあるファイルは、Apple独自のドキュメントAPI（`NSDocument`等）を使っていない、一般的なアプリが書き込む普通のファイルであっても、オフライン分岐（2台のデバイスがオフラインのまま同じファイルを編集し、後でオンラインに戻る）が起きた場合は**両方のバージョンを保持する**。具体的には、片方を元のファイル名のまま、もう片方を「ファイル名 2.md」のような別名の新しいファイルとして複製する（いわゆるbounced copy）。データを黙って一方だけ残して消す（silent discard）のは、Filesアプリに見えない「dataスコープ」（アプリ専用の非公開領域）に限られ、Vaultのような可視フォルダには当てはまらない（出典：Apple Technical Note TN2336、Apple公式サポート文書）。

**ただし、この保証はこのアプリの3-way mergeとは独立に、iCloud自身が行うものである。** ObsidianがVaultをiCloud Drive上に直接置いて編集している現行方式では、iCloudの内部デーモンによるこの独自コンフリクト処理と、このアプリ自身のスキャン・3-way mergeエンジン（3.3節・3.4節）が、**同じVaultファイルに対して並行して働き得る**。データが失われるわけではないが（iCloud側は上記の通り両方保持する）、このアプリが正しく3-way mergeで統合した結果とは別に、iCloudが作った「ファイル名 2.md」のような重複ファイルがVault内に残ってしまう可能性があり、ユーザーに混乱を与える。

**iPhoneとの橋渡しに関する重要な制約：** iPhoneはこのアプリを実行できない（iOSのバックグラウンド実行制限、1.4節）。そのため、iPhone側の編集はこのアプリの`_sync/`ログに一切記録されず、このアプリの3-way mergeエンジンの対象にもならない。iPhone⇔Mac間のオフライン分岐は、常にiCloud自身の上記コンフリクト処理（bounced copy）に委ねられる。**この依存は、クラウドストレージの種類を変えても解消できない、iOSプラットフォーム自体の制約である。**「Vaultデータをshareする以上、iPhone絡みの分岐はこのアプリの制御が及ばない既知の問題として受け入れ、影響範囲を最小化する」という方針を取る（1.6節参照）。

**方針転換：** 上記の理由により、1.6節の通りアーキテクチャを移行する。Vault本体はローカル専用フォルダへ移し、Obsidian自身の編集がiCloudの内部処理と競合するリスクを排除する。iPhoneとの橋渡しは「iCloud Bridge」という専用のステージング領域に限定し、その領域だけがiCloud側の既知の制約（bounced copy）の影響を受け得る状態にする。PC間（Mac⇔Windows等）の同期は、このアプリが完全に制御できるGoogle Drive（rclone）へ移す。

#### 3.6.2 改行コード（CRLF/LF）の正規化
Windows環境のエディタや `core.autocrlf` 設定により、内容が同じでも改行コードだけが変わることがある。これを放置すると、実際には変更していないファイルが「変更あり」と誤検知され、偽の競合が多発する。

- ハッシュ計算・diff生成の**直前に、全ファイルの内容をLFへ正規化**してから比較する。
- Vault現物ファイルへの書き戻し時も、改行コードの方針を統一する（LF固定を推奨。Obsidianの表示・編集には影響しない）。

#### 3.6.3 その他のOS差分に関する注意点
- **パス区切り文字**：Go標準の `path/filepath` パッケージを使い、OSごとの `/` と `\` の差異を吸収する。
- **ファイル名の大小文字**：Windows/macOSの標準ファイルシステムは大文字小文字を区別しない設定が一般的なため、大文字小文字のみが異なる同名ファイルを作らないよう運用上の注意を促す（自動検出は任意）。

#### 3.6.3.1 Git と rclone、それぞれ実際にいつ必要になるか

Git・rclone とも自動インストール対応だが（3.6.4節）、**両者は必要になる条件が異なる**。誤解しやすい点のため明記する。

- **Git（`git merge-file`）が実際に実行されるのは、2台以上のデバイスが同じファイルを分岐点から変更していた場合のみ**（3.3節）。`internal/merge`のN-wayマージは対象パスの版が1つしかない場合は早期リターンしてgitを一切呼び出さず、`internal/engine`のマージフェーズもログが1台分しかないパスはその時点でスキップする。したがって、**編集端末が1台のみの構成では、gitは実行時には一度も呼び出されない**。
  - 例：Windows 1台のみでObsidianを使い、Google Driveへのバックアップだけしたい場合 → gitは不要、rcloneのみで足りる。
  - 例：Mac＋iPhone、または複数台のWindowsで同期する場合 → 端末間の競合解決にgitが必要。
- **rclone（Google Drive転送）は、台数に関わらずGoogle Driveバックアップを使う設定であれば必須**。加えて、**複数端末構成では Primary/Secondary の役割判定自体が Google Drive 上の `PRIMARY_MARKER.json` の有無を基準に行われるため（3.6.1.1節）、rcloneはバックアップ機能だけでなく複数端末構成の成立条件そのものでもある**。「バックアップは要らないが端末間の同期・競合解決だけ使いたい」という構成は、現状の実装ではサポート対象外（役割判定の代替手段が無いため）。

上記の非対称性を踏まえ、事前チェック（Preflight Checks、8.2節）の扱いも意図的に非対称にしている：

- **Git**：`unitevault init` / `unitevault run`（CLIサブコマンド）は、未検出の場合は台数に関係なく即座にエラー終了する（実行時に一度もgitを呼ばない構成であっても、起動時点では一律で必須として扱う）。将来デバイスを追加した際に改めてインストールを求められる手間を避けるための単純化。GUI（トレイ／Settingsウィンドウ）は起動やSettings画面表示自体はブロックせず、Statusセクションから任意のタイミングでインストールできる（3.5.2節）。
- **rclone**：GUI・CLIともに、未検出でも起動やSettings画面表示自体はブロックしない。`drive.NewClient`は未検出時にユーザー設定領域へ自動ダウンロードを試みるのみで、これは「Save Settings」実行時や「Configure Google Drive Remote...」実行時など、実際にGoogle Driveへ接続する操作が行われたタイミングで初めて発火する。

#### 3.6.4 各OSで必要なインストール要件と自動セットアップ

UniteVault は事前チェック機能（Preflight Checks）を備えており、以下のツールが未インストールの場合は起動時・設定時に自動ダウンロード・自動インストール処理をトリガーする。

**Mac側：**
| 項目 | 用途 | 自動インストール機能 |
|---|---|---|
| Git | `git merge-file` コマンドの利用 | 未検出時、Homebrew (`brew install git`) または Xcode Command Line Tools のシステムダイアログ (`xcode-select --install`) を自動起動 |
| rclone | Google Driveへのミラー転送 | 未検出時、ユーザー設定領域 (`~/.unitevault/bin/rclone`) へ公式最新バイナリを自動取得・解凍配置 |
| 同期エンジン (`UniteVault.app`) | 高コントラスト常駐メニューバーGUI | 単一バンドル。アドホック署名済み |

**Windows側：**
| 項目 | 用途 | 自動インストール機能 |
|---|---|---|
| Git for Windows | `git merge-file` コマンドの利用 | 未検出時、`winget install --id Git.Git` を試行し、不可の場合は公式 `Git-Installer.exe` を一時フォルダへ自動取得・起動 |
| rclone | Google Driveへのミラー転送 | 未検出時、ユーザー設定領域 (`%APPDATA%\unitevault\bin\rclone.exe`) へ公式最新バイナリを自動取得・解凍配置 |
| 同期エンジン (`UniteVault.exe`) | システムトレイ常駐GUI | ZIP形式ポータブル版（`UniteVault-windows-amd64.zip`）で配布 |
| iCloud for Windows | Mac/iPhone/iPadとのVaultフォルダ共有（iCloud Drive） | 未検出時、まず`winget install --id 9PKTQ5699M62 --source msstore`（Microsoft Store版）を試行する。Store版はユーザー権限のみでインストールでき、管理者権限（UACダイアログ）を必要としないため優先する。失敗した場合（Store未サインイン・企業ポリシー等）のみ、`winget install --id Apple.iCloud`（クラシック版。管理者権限が必要）にフォールバックする。インストール後はApple ID サインイン・iCloud Drive有効化への誘導としてStart Menuショートカット経由でアプリを自動起動するが、それ以降（サインイン・2FA）は手動操作が必須 |

#### 3.6.5 実装言語としてGoを採用する理由
- 単一の静的バイナリにクロスコンパイルできるため、Mac/Windowsとも「実行ファイルを置くだけ」で動き、Python等のようにランタイムやパッケージ管理（pip等）の整備が不要。
- macOSのOS標準Pythonは将来的に同梱されなくなる可能性がAppleにより示唆されており、長期運用の土台として不安定。Windowsのembeddable版Pythonもpipが使えない等の制約があり、いずれも今回の要件（低メンテナンスで長期運用したい）に対して相性が悪い。
- 今回の処理内容（ハッシュ計算・JSON Lines読み書き・外部コマンド呼び出し）は標準ライブラリ（`crypto/sha256`, `encoding/json`, `os/exec`, `path/filepath` 等）のみで実装可能であり、外部依存パッケージの管理コストも発生しない。
- Rustと比較すると、所有権システム等による学習・実装コストが低く、今回のような「定期実行バッチ＋外部コマンド呼び出し」中心の処理には過剰装備にならない。

#### 3.6.6 ビルド・配布方法
- **GitHub Actions**で Mac用・Windows用バイナリをクロスコンパイルし、**GitHub Releases**で配布する（機密データ自体はGitHubを経由しないため、1.2節の「SaaSを経由させない」方針とは抵触しない。配布されるのはツールのバイナリのみ）。

##### 3.6.6.1 Mac向けバイナリ：「壊れている」エラー対策（アドホック署名を必須工程にする）
Apple Silicon（M1以降）のMacでは、**署名が全く無い実行ファイルは、Gatekeeperの警告表示すら出ずに「"（アプリ名）"は壊れているため開けません。」というエラーで起動自体を拒否**される。これはGatekeeperの警告（＝開けるが確認が要る）とは別の問題で、Apple SiliconがOSレベルで「有効な署名（有償の証明書でなくてよい）」を要求するために起きる。

これを避けるため、**ビルドパイプラインに、有償のDeveloper Program証明書を使わない「アドホック署名（ad-hoc signing）」を必須工程として組み込む。**

**GitHub Actions（macosランナー）でのビルド手順：**
```bash
# 1. Goでクロスコンパイル
GOOS=darwin GOARCH=arm64 go build -o unitevault-mac-arm64 .
GOOS=darwin GOARCH=amd64 go build -o unitevault-mac-amd64 .

# 2. アドホック署名（証明書不要。署名者を "-" にするのがアドホック署名の指定方法）
codesign --force --deep --sign - unitevault-mac-arm64
codesign --force --deep --sign - unitevault-mac-amd64

# 3. 署名が有効か検証（CIのテスト工程に含める）
codesign --verify --verbose unitevault-mac-arm64
```
- macOSランナー上には`codesign`が標準で入っているため、追加の証明書購入・登録なしでこの手順を実行できる。
- これにより「壊れている」エラーは解消される。ただし、**ブラウザ経由でダウンロードした場合に付与される`com.apple.quarantine`属性による「開発元が未確認のため開けません」という警告（Gatekeeper本来の確認ダイアログ）は別途残る**ため、初回起動時のみ以下のいずれかの操作が必要である旨、セットアップ手順に明記する。
  - 右クリック（Controlキー+クリック）→「開く」を選択し、確認ダイアログで「開く」を選択
  - または `xattr -d com.apple.quarantine <バイナリ>` を一度だけ実行して検疫属性を除去する

##### 3.6.6.2 Windows向けバイナリ
- Apple Silicon Macのような「署名必須」の制約は無いが、SmartScreenの警告は表示される。Apple Developer Program相当の証明書購入は今回は必須にしない。
- 初回実行時、SmartScreenの警告画面で「詳細情報」→「実行」を選択する旨、セットアップ手順に明記する。
- Settings/トレイGUIはFyne（`fyne.io/fyne/v2`）採用のためCGO必須（8.3節参照）。macOSランナーからWindows向けにクロスコンパイルする際は、`brew install mingw-w64` で取得できる `x86_64-w64-mingw32-gcc` をCコンパイラに指定し、`CGO_ENABLED=1` でビルドする（`release.yml`参照）。

##### 3.6.6.3 セットアップ手順への反映
上記の回避策（Mac：右クリック開く or `xattr -d`、Windows：詳細情報→実行）は、**初回セットアップ手順書（README等）に必須項目として明記する**。CIでのアドホック署名により「壊れている」エラー自体は解消されるが、検疫属性による確認ダイアログは製品版の有償署名・公証（3.6.6.4節）を導入しない限り残るため、ユーザーへの案内を省略しないこと。

##### 3.6.6.4 将来の拡張（Todo）
- Apple Developer Program（年会費）での正式な署名・公証（notarization）を導入すれば、Gatekeeperの確認ダイアログ自体も出なくなる。個人利用の範囲では必須ではないが、5節のTodoに検討事項として記載する。

---

## 4. 運用フロー（cron / タスクスケジューラ想定）

| ステップ | 内容 | 実行主体 |
|---|---|---|
| 1 | iPhone・各Windows PCでの編集がiCloud経由でプライマリノードに反映されるのを待つ | iCloud（自動） |
| 2 | Vault内の変更検出（改行コード正規化後にハッシュ再計算。2回連続で変化がないファイルのみ処理対象とする／3.4.1節） | プライマリノードの定期処理 |
| 3 | 変更を自デバイスのログ（`log-<UUID>.jsonl`）に追記（リネームはハッシュ一致で検出／3.3.0節） | 同上 |
| 4 | 存在する全デバイスのログを突き合わせ、3-way merge実行（3台以上は繰り返し適用） | 同上 |
| 5 | 競合があればユーザーに通知・選択を促す | 同上（通知） |
| 6 | マージ後の状態を `rclone sync` でGoogle Driveへ反映（失敗時は指数バックオフで再試行／3.5.1節） | 同上 |

---

## 5. 将来タスク（Todo・v1の実装には影響しない先送り事項）

v1の実装に必要な設計判断はここまでで完了している。以下は意図的に「初期バージョンは最小構成、将来拡張する」と決めた事項であり、実装開始のブロッカーではない。

- **コンテンツ差分ログ（3.2節、`log-<uuid>.jsonl`）の圧縮**：初期バージョンでは圧縮なし。マージのbase復元に使う情報（3.4節）と絡むため、圧縮対象から除外すべき直近エントリの判定を含め、具体的なアルゴリズム・実行タイミングを実装する必要がある（3.2.1節のアプリケーションイベントログは既に365日保持＋自動間引きを実装済みだが、こちらは対象外）。
- **真のunified diff形式への移行**：初期バージョンでは3.4節の通り、baseの復元用にログの`diff`フィールドへファイル全文を保存する簡略実装としている。将来的に本来のunified diff形式へ移行し、保存量を削減する。
- **デスクトップ通知への対応**：現状、多重Primary衝突（3.6.1.4節）・未解決コンテンツ競合（3.3.2節）ともSettings画面を開かないと気づけない。将来、通知センター／トースト通知等でプロアクティブに知らせる。
- **iCloud for Windowsの導入・動作確認**：実機での動作検証（Mac版との挙動差の有無）を今後行う。
- **バイナリの正式署名・公証（notarization）**：現状はアドホック署名＋検疫属性除去の案内で「壊れている」エラーのみ回避（3.6.6.1節）。Gatekeeper確認ダイアログ自体を消すには、Apple Developer Program（年会費）での正式署名・公証が必要。将来的に導入するかどうかを検討する（3.6.6.4節）。

---

## 6. ファイル構成

### 6.1 Vault内の構成（iCloud上、Mac/Windows/iPhone共通で見える階層）

```
Vault/                              ← Obsidianが直接扱うVaultルート（既存のまま）
├── (これまで通りのノート・添付ファイル群を自由に配置)
└── _sync/                          ← 同期システム専用ディレクトリ（新規追加）
    ├── log-<device-uuid-A>.jsonl      ← Mac のログ（3.2節）
    ├── log-<device-uuid-B>.jsonl      ← iPhone のログ
    ├── log-<device-uuid-C>.jsonl      ← Windows機A のログ
    ├── log-<device-uuid-D>.jsonl      ← Windows機B のログ（台数分増える）
    ├── events-<device-uuid-A>.jsonl   ← Mac のアプリケーションイベントログ（3.2.1節）
    ├── events-<device-uuid-B>.jsonl   ← 同様に台数分増える
    ├── PRIMARY_MARKER.json            ← プライマリノードの判定情報（3.6.1.1節。詳細は6.3節の注意点を参照）
    └── state/
        ├── last_scan.json          ← 前回の生スキャン結果（デバウンス比較専用。3.3.0節・3.4.1節）
        └── last_confirmed_scan.json ← 変更検出（ログ記録）の比較基準となる確定済み状態（3.4.2節）。last_scan.jsonとは別に保持する
```

- `_sync/` はObsidianのノートと同じVault内に置くことで、iCloud同期にそのまま乗る（Windows/iPhoneへも自動的に配布される）。
- `state/`配下の2ファイルは各デバイスの作業ファイルであり、他デバイスと共有する必要はない。Primary・Secondaryいずれもスキャン・ログ記録を行うため両方が書き込む（3.3節）が、実装簡略化のため配置場所はVault内`_sync/`とする（他デバイスは無視してよい）。
- `PRIMARY_CONFLICT.json`（3.6.1.4節）は**ここには置かない**。理由は6.3節を参照。

### 6.2 Google Drive上のバックアップ構成（rcloneのミラー先）

```
GoogleDrive:VaultBackup/            ← rclone sync の転送先フォルダ
├── (Vaultの内容一式のミラー：ノート・添付ファイル)
└── _sync/
    ├── log-<device-uuid-A>.jsonl      ← 全デバイスのログのミラー
    ├── log-<device-uuid-B>.jsonl
    ├── events-<device-uuid-A>.jsonl   ← 全デバイスのイベントログのミラー
    ├── events-<device-uuid-B>.jsonl
    ├── ...
    ├── PRIMARY_MARKER.json            ← 6.3節の注意点を参照
    └── PRIMARY_CONFLICT.json          ← 多重Primary衝突が発生している間のみ存在（3.6.1.4節）。ミラー転送ではなく都度直接アップロード/削除される特殊ファイル（6.3節参照）
```

- Google Drive側は3.5節の通り、人間が直接編集しない**読み取り専用に近いバックアップ先**として扱う。

### 6.3 注意点：`PRIMARY_MARKER.json` はローカルVault側にも複製を残すこと（重要）
`rclone sync` は**ミラー転送**（転送元に存在しないファイルは転送先から削除する）であるため、もし`PRIMARY_MARKER.json`をGoogle Drive側だけに置き、ローカルの`_sync/`に対応するファイルを置かなければ、**次回の`rclone sync`実行時にGoogle Drive側のマーカーファイルごと削除されてしまう**（プライマリの初期化情報が消え、次回起動時に「マーカーが存在しない」と誤判定して二重初期化される事故につながる）。

そのため、**プライマリノードは`PRIMARY_MARKER.json`をローカルのVault内`_sync/`にも同じ内容で保持し、通常のVaultファイルと同様に`rclone sync`でミラーされる対象に含める**。3.6.1.1節の初期化フローに、この複製手順を含めるものとする。

**`PRIMARY_CONFLICT.json`（3.6.1.4節）はこの複製ルールの対象外：** このファイルは、書き込む側のデバイス（マーカーに超された、または他デバイスから衝突を通知されたデバイス）自身は`rclone sync`を一切実行しない状態にあり、`rclone`の`copyto`／`deletefile`で直接Google Drive上のみを操作する。ローカルVault側の`_sync/`には複製を置かない。仮に複製を置いてしまうと、**衝突が解決されるまでの間、現Primary側のデバイスも`rclone sync`を停止している**（3.6.1.4節の設計）ため、`PRIMARY_MARKER.json`のような「複製しておかないとミラー転送で消される」問題自体が起こり得ない。むしろ複製を置くと、衝突解決後に通常の`rclone sync`が再開した際、ローカルに残った古い複製がGoogle Drive側に再アップロードされてしまう不具合の方が心配になるため、意図的に複製しない。

### 6.4 デバイスローカルの設定領域（Vault外、iCloud同期対象外）

各デバイスは、Vaultとは別に、そのデバイス固有の設定を保持するローカル領域を持つ。

```
Mac:      ~/.unitevault/
Windows:  %APPDATA%\unitevault\

├── config.json             ← Vaultのパス、rcloneのリモート名・バックアップ先パス、実行間隔等の設定
├── device_id                ← このデバイス固有のUUID（3.2節）
├── role                      ← 自動判定結果のキャッシュ（"primary" / "secondary"、3.6.1.1節）
├── drive_sync_status.json    ← 直近のGoogle Drive同期結果のキャッシュ（Settings > Statusの表示用）
├── primary_conflict.json     ← 多重Primary衝突の検出状況のローカルキャッシュ（存在する間のみ。3.6.1.4節。Google Drive上の`PRIMARY_CONFLICT.json`の内容を都度取りに行かず、Settings画面の警告表示をこのキャッシュから即座に描画するためのもの）
├── pending_conflicts.json    ← 未解決のコンテンツ競合（真の競合）一覧のローカルキャッシュ（存在する間のみ。3.3.2節）。Primaryのみが持つ。他デバイスとは共有・同期しない
└── engine.log                ← 実行ログ（rclone失敗・コンフリクト記録等。3.5.1節・3.5.2節の出力先）
```

- ここはVaultの外（iCloud同期対象外）に置く。デバイスごとに固有の情報であり、他デバイスと共有すべきではないため。

### 6.5 ツールのソースコード構成（Go実装）

```
unitevault/                         ← ソースコードリポジトリ（GitHub、3.6.6節）
├── cmd/
│   └── unitevault/
│       └── main.go                 ← エントリポイント
├── internal/
│   ├── scan/                       ← ハッシュ計算・変更検出・リネーム推測（3.3.0節）
│   ├── syncedlog/                  ← デバイス別JSON Linesログの読み書き（3.2節）
│   ├── eventlog/                   ← デバイス別アプリケーションイベントログの読み書き・間引き（3.2.1節）
│   ├── merge/                      ← git merge-file 呼び出し、N-way merge（3.3節）
│   ├── bootstrap/                  ← プライマリ／セカンダリ自動判定、継続的検証・多重Primary衝突の検出/解決（3.6.1.1節・3.6.1.4節）
│   ├── drive/                      ← rclone 呼び出しラッパー、リトライ制御（3.5節）
│   ├── config/                     ← ローカル設定ファイル（6.4節）の読み書き
│   ├── gui/                        ← Settingsウィンドウ・ダイアログ（Fyne。3.5.2節・8.3節）
│   └── selfupdate/                 ← GitHub Releasesからの自己アップデート（8.4節）
├── .github/
│   └── workflows/
│       └── release.yml             ← クロスコンパイル・アドホック署名・GitHub Releases公開（3.6.6節）
├── go.mod
└── README.md                       ← セットアップ手順（初回インストール、署名回避策の案内等／3.6.6.3節）
```

---

## 7. 不採用とした代替案とその理由（参考）

| 案 | 不採用理由 |
|---|---|
| gitをiCloudフォルダ内で直接運用 | `.git`内部ファイルとiCloudのオンデマンドダウンロード機構の相性が悪く、破損リスクがある |
| GitHub等SaaSリポジトリの利用 | 機密データのため社外SaaSを経由させたくない |
| PlaywrightによるGoogle Drive Web UI自動操作 | UI変更に弱い、規約上のリスク、git転送プロトコルとして機能しない、iOS上で実行不可 |
| Syncthingの導入 | Mac⇔iPhone間の同期は既にiCloudが担っており、Syncthing独自の競合解決（`.sync-conflict`）と自作ログ機構が二重に働き複雑化する。iOS版が非公式で有料アプリ経由になる点も負担増 |
| rclone bisync（双方向同期） | ベータ品質であり、Google Drive側を人間が直接編集しない運用のため、双方向は過剰。一方向の`sync`で要件を満たせる |

---

## 8. GUI・デザイン・事前チェック詳細仕様

### 8.1 メニューバー / システムトレイアイコン仕様
- **デザイン方針**: 3.5.2節の通り、モチーフは循環する2本の矢印（同期を想起させる図形）。macOSメニューバーはOS標準に合わせたモノクロ・テンプレート画像、Windowsタスクトレイおよびアプリアイコン本体はパープルを使った高コントラスト配色とする。
- **アセット構成**（`scripts/gen_assets.sh` で再生成可能。ソースは `scripts/gen_icon.swift` によるCoreGraphicsでのベクター描画）:
  - macOS App アイコン一式: `assets/icon.iconset/*.png` → `assets/AppIcon.icns`（パープル背景＋白矢印）
  - トレイ用アイコン（カラー版・Windows/Linux用 兼 アプリ/ウィンドウアイコン）: `assets/tray/icon.png` (32x32), `assets/tray/icon@2x.png` (64x64)
  - トレイ用アイコン（モノクロ版・macOSメニューバー専用）: `assets/tray/icon-mono.svg`。PNGではなくSVGを使うのは、FyneのThemedResourceが内部でSVGとして色置換を試みるため（PNGを渡すと置換に失敗し警告ログを出したうえでフォールバックする）。`width`/`height`属性を省略すると一部のSVGレンダラでスケーリングが崩れるため、`viewBox`と同じ値を明示的に指定すること。
  - Windows用アイコンリソース: `assets/tray/icon.ico`（16/32/48/256pxを含むマルチ解像度、カラー版から生成）

### 8.2 事前環境チェック（Preflight Checks）と依存管理
アプリケーション起動時および初期設定・設定変更時に、動作に必要な外部コマンドの事前判定を行う。Git と rclone は実際に必要となる条件が異なり、チェックの厳しさも意図的に非対称にしている（詳細は3.6.3.1節）。

未初期化（Vault未設定）の状態で、Git または rclone が未検出のままアプリを起動した場合、Settings ウィンドウが自動的に開くことに加えて、**起動のたびに「Setup Required」リマインダーダイアログ**を表示し、不足しているツールをまとめて案内する。ユーザーが「Don't show this again」にチェックを入れて閉じた場合のみ、以降の起動でこのリマインダーを抑制する（抑制設定はローカル設定領域のマーカーファイルに保持し、Reset Configuration 実行時にクリアされる）。Git・rclone の両方が検出された時点で、抑制設定の有無に関わらずリマインダー自体が不要になり自動的に表示されなくなる。

1. **Git の自動検知と自動インストール機能**:
   - システム `PATH` および標準インストールパス（`C:\Program Files\Git\cmd\git.exe` や `/usr/local/bin/git`, `/opt/homebrew/bin/git` 等）で `git` の存在を動的チェックする。
   - 未検出の場合、ダイアログで「Gitを自動インストールしますか？」と提示する。
     - **macOS**: Homebrew (`brew install git`) または Apple Developer Command Line Tools のシステムインストールダイアログ (`xcode-select --install`) を自動起動。
     - **Windows**: Windows Package Manager (`winget install --id Git.Git --silent`) を試行し、不可の場合は公式 Git インストーラー (`Git-Installer.exe`) を一時フォルダへ自動ダウンロードしてサイレント/GUIインストーラーを自動起動。
   - 自動インストールが辞退・失敗した場合は、公式ダウンロードページ（`https://git-scm.com/download/mac` / `https://gitforwindows.org/`）のブラウザ案内を提供する。
2. **rclone の自動検知・自動ダウンロード**:
   - システム `PATH` およびユーザーローカル領域（`~/.unitevault/bin/rclone` または `%APPDATA%\unitevault\bin\rclone.exe`）を検索する。
   - 未検出の場合、アプリバンドル内ではなく**書き込み権限が確実に存在するユーザー設定領域**（`~/.unitevault/bin/`）へ、OS/アーキテクチャに応じた最新の official `rclone` バイナリを自動ダウンロード・解凍配置するダイアログを提示する。
   - 自動ダウンロード失敗時またはユーザー拒否時は、公式ダウンロードページ（`https://rclone.org/downloads/`）の案内ブラウザリンクを提供する。
3. **iCloud for Windows の自動インストール（Windows環境）**:
   - Windows側でiPhone（iOS）との同期を行う場合、Vaultフォルダが iCloud Drive 配下にある必要があるため、初期設定時（Vault未設定かつiCloud未検出の場合）に確認ダイアログを表示し、承諾されれば`AutoInstallICloud`（3.6.4節）でその場でインストールを実行する（以前はダウンロードページへのリンクを提示するだけだったが、Settings > Statusの `Install iCloud...` ボタンと同じ実インストール動作に統一した）。

### 8.3 設定画面（GUI Settings Window / Setup Wizard）仕様
メニューバーの「Settings...」選択時および未初期化時の起動フロー：

- **設定一覧表示および rclone リモート接続状態の確認**:
  - すでに設定済みの状態で「Settings...」を開いた場合も、単一ウィンドウ（3.5.2節）にその場で現在値を反映して表示する（ステップ形式の要約ダイアログは介さない）。表示される項目は次の通り：
    1. Vault ディレクトリパス（Obsidian Vault セクション）
    2. rclone リモート名 (`rclone_remote`) と**接続設定状態**（`rclone listremotes` を実行し、指定リモートが rclone 側に登録済みか動的チェック）、rclone バイナリの参照パス、Google Drive バックアップ先パス (`rclone_path`)、同期インターバル秒数 (`interval_seconds`) （いずれも rclone セクション。3.5.2節）
    3. ノード役割 (Primary / Secondary)（Status セクション）
  2. **rclone リモート名設定と検証**: テキスト入力ダイアログ（デフォルト: `ObsidianVault`）。入力されたリモート名が `rclone listremotes` に存在しない場合、ダイアログで「新規Rclone設定（デフォルト、GUIのみで完結）」と「既存/カスタムRclone設定（Terminal/PowerShell）」をユーザーに選択させる。「新規Rclone設定」選択時は `rclone config create <remote> drive` を非対話実行して自動的にブラウザでのGoogle認証ページを開き、コマンド操作不要で設定を完了する。「既存/カスタムRclone設定」選択時は Terminal (macOS) や PowerShell (Windows) で `rclone config` を対話起動する。
  3. **Google Drive バックアップ先パス設定**: rclone セクション内のテキスト入力（デフォルト: `VaultBackup`）。
  4. **設定保存とノード初期化**: `config.json` への保存およびノード役割判定（`PRIMARY_MARKER.json`の検証）を自動実行し、完了ダイアログを表示する。

### 8.4 自己アップデート機能
メニューバー／タスクトレイの「Check for Update...」を選択すると、GitHub Releases（`GET /repos/kh813/unitevault/releases/latest`）から最新版を取得し、現在のバージョン（`bootstrap.AppVersion`）と比較する。バージョン比較はドット区切りの数値として行う（`0.0.9` < `0.0.10` を文字列比較ではなく正しく数値として判定する）。

- **最新版が無い場合**: 「You're running the latest version」を表示して終了する。この種のタスクトレイ発の通知ダイアログは、Settings画面が閉じている状態で表示されても閉じた後に空の（または前回開いていた古い内容が残る）ウィンドウが表示されたままにならないよう、ダイアログ表示のために共有ウィンドウを開いた場合はダイアログが閉じるタイミングで再度隠す（元々開いていた場合は隠さない）。
- **最新版があり、対応プラットフォーム向けの成果物（`UniteVault-mac-arm64.app.zip` / `UniteVault-windows-amd64.zip`）が見つかった場合**: ダウンロード・自動適用・再起動を行うか確認ダイアログを表示する。承諾された場合のみ以下を実行する。
- **最新版はあるが対応する成果物が見つからない場合**: Releaseページをブラウザで開くか確認する（自動更新は行わない）。

**自己更新の適用方式（OSごと）**：自プロセスは実行中の自分自身を安全に置き換えられないため、実際のファイル置換・再起動は**検知用に切り離したヘルパープロセス**（macOS: シェルスクリプト、Windows: バッチファイル）に委譲する。呼び出し元のGoプロセスはヘルパー起動後、直ちに終了する。

- **macOS**: 実行中のバイナリパスから`.app`バンドルのルートを特定し（`.app`バンドル外（`go run`等の開発時実行）の場合はエラーとしてフォールバックする）、新しい`.app`一式をダウンロード・zip展開（ファイルモード＝実行ビットを保持したまま展開し、アドホック署名がバイト単位で維持されるようにする）した上で、`sleep 1; rm -rf 旧.app; mv 新.app 旧.appの場所; open 旧.appの場所` 相当のシェルスクリプトをデタッチして起動する。パスはシェルスクリプト本文への文字列埋め込みではなく、位置引数（`$1`/`$2`/`$3`）として渡し、スペースや特殊文字を含むパスでも安全に扱う。
- **Windows**: 実行中の`.exe`を直接上書きできないため、新しい`.exe`を`UniteVault.exe.new`として同一フォルダにダウンロードし、旧プロセスをPIDで強制終了→リネーム（旧→`.old`、新→正式名。ロック解除待ちのため最大10回リトライ）→再起動、を行う**バッチファイル（`.bat`、`cmd.exe`経由で実行）**を一時フォルダに書き出し、位置引数（`%1`〜`%4`）でパスとPIDを渡してデタッチ起動する。**PowerShellではなくバッチファイルを使う**のは、PowerShellスクリプトが実機で`-ExecutionPolicy Bypass`指定でも実行されない環境（グループポリシー等）があり、その場合スクリプトの中身に関わらず一切実行されず`UniteVault.exe.new`だけが残る不具合が実際に発生したため。バッチファイル（`cmd.exe`）にはPowerShellの実行ポリシーに相当する制限がなく、この問題を回避できる。
- **上記以外のOS**：自己更新は非対応（エラーを返す）。

**失敗時のフォールバック**: ダウンロード・展開・ヘルパー起動のいずれかで失敗した場合、ヘルパープロセスは一切起動されない（＝既存のインストール状態は変化しない）ため、エラーメッセージとともにReleaseページへのリンクを提示し、手動ダウンロードを案内する。自動更新が「中途半端に失敗してアプリが起動できなくなる」状態を作らないことを設計上の必須要件とする。

### 8.5 簡易i18n（多言語対応）

OSS公開に伴い、UIの文言（ダイアログ・ボタン・ステータス表示等）を多言語対応する。対象は**アプリ独自のUI文言のみ**であり、`unitevault-spec.md`本文や`README.md`等のドキュメント類は対象外（ドキュメントは今後も日本語・英語をそれぞれ別ファイルとして維持する運用とし、自動翻訳の対象にはしない）。

#### 8.5.1 採用方式：Fyneの標準i18n機構（`fyne.io/fyne/v2/lang`）をそのまま利用

新規の外部依存追加やフレームワーク・実装言語の変更は行わない（8.5.4節に検討経緯を残す）。Fyneには`fyne.io/fyne/v2/lang`パッケージが標準で同梱されており、これをそのまま利用する。

- 内部実装は Go 製 i18n ライブラリの定番である `github.com/nicksnyder/go-i18n/v2`（翻訳バンドルの読み込み・プレースホルダ・複数形処理）と `github.com/jeandeaual/go-locale`（OSのロケール自動検出）で構成されており、すでにFyne自体の組み込みウィジェット（`dialog.NewConfirm`の「Yes」「No」ボタン等）のローカライズに使われている。UniteVault独自の文言にもこの同じ仕組みを拡張して使うだけで済み、`go.mod`への新規追加は発生しない。
- 翻訳キーの指定方法は gettext ライクな `lang.L("英語の原文そのもの")` を基本とする（`Localize`のショートカット）。原文自体をキーとして使うため、既存の英語文字列リテラルを`lang.L("...")`で包むだけで移行でき、キー名を別途設計する必要がない。
- 翻訳データは `github.com/nicksnyder/go-i18n` 形式のJSONファイル（`{"英語の原文": "翻訳文", ...}`）とし、`internal/gui/translations/*.json`（ファイル名がロケールコードに対応。例：`ja.json`）に配置、`//go:embed`でバイナリに埋め込み、起動時に`lang.AddTranslationsFS(...)`で読み込む。
- **表示言語の決定**：OSのロケール設定から`go-locale`が自動検出し、対応する翻訳ファイルがあればそれを使用、無ければ英語（原文）にフォールバックする。ユーザーがアプリ内で手動切り替えする設定項目は設けない（初期バージョンでは不要と判断。将来必要になれば5節のTodoに追加）。

#### 8.5.2 適用範囲

- **対象**：`internal/gui`パッケージおよび`cmd/unitevault/main.go`内の、ユーザーに表示されるUI文言全て（ボタンラベル・ステータス表示・ダイアログのタイトル/本文・エラーメッセージ等）。
- **対象外**：
  - ログファイル（`engine.log`）やコード内コメント、CLIサブコマンド（`unitevault status`等のターミナル出力）は初期バージョンでは対象外とする（開発者・技術的ユーザー向けの経路であり、GUIほど優先度が高くないため）。
  - 外部プロセス（`winget`、`rclone`、OS標準のUACダイアログ等）自体が出力するメッセージはUniteVaultの管轄外であり翻訳しない。

#### 8.5.3 翻訳ファイルの初期整備・将来のコミュニティ翻訳追加

- 初期バージョンでは日本語（`ja.json`）のみを追加する。英語は原文（フォールバック）として扱うため、`en.json`は用意しない。
- OSS化後、他言語の翻訳ファイルは`internal/gui/translations/`に対応する`<locale>.json`を追加するだけで対応できる（コード変更不要）。コントリビューションガイド（README等）にこの追加方法を明記する。

#### 8.5.4 （参考）検討した代替案とその理由

| 案 | 不採用理由 |
|---|---|
| i18n対応した別のGoフレームワークへの書き換え | Fyneに標準のi18n機構（8.5.1節）が既に存在し利用可能なため、フレームワーク移行のコスト（Windows/macOS固有の実装：ネイティブフォルダダイアログのCOMスレッド対応、自己更新ヘルパー、アイコン埋め込み等、8.4節・3.6.6節参照）に見合うメリットが無い |
| Rustへの書き換え＋Rustのi18nライブラリ利用 | 3.6.5節でRustを不採用とした理由（所有権システム等の学習・実装コストが本用途には過剰）がそのまま当てはまる上、GUI層を含めた全面書き換えとなり最もコストが高い |
